# 木脉智检平台视觉重构基线

版本：1.1  
更新日期：2026-09-12  
配套规范：视觉设计规范-v1.1.md

## 1. 基线范围

本版保留原文件的历史测量，并补充2026-09-12平台评审记录，作为下一轮重构的起点。本次更新文档没有重新运行全站测试；“本轮记录”指前一轮已完成的浏览器检查，不代表此刻持续变化的源码已全部复测。

- 前一轮服务：http://localhost:5173/，Hash路由。
- 首轮基线：2d2d4f3及当时未提交的交付中心等修改。
- 收尾基线：7bd8577及随后新增的Spark／SOG修改；孪生页补查至19:32。
- 账号：沈、史、饶、马四个账号均已登录检查。
- 覆盖：9个主页面、手持和模型附加页签、知识库空间／分块／检索视图。
- 尺寸：主要1920×1080；补查1366×768工单、知识库、模型页。
- 原文件中的5199端口、`#/adapt`与`#/console`属于历史记录，不能当作当前可用入口。

## 2. 当前路由与检查范围

| 路由 | 页面 | 说明 |
|---|---|---|
| `#/` | 任务总览 | 全国地图与业务摘要 |
| `#/orders` | 工单档案 | 环境作业、配置和记录 |
| `#/mapping` | 建图巡检 | 地图、视频与任务 |
| `#/twin` | 数字孪生 | 包含收尾新增SOG加载补查 |
| `#/hardware` | 手持页面 | 导航建议改为采集作业；含采集、排查、监视 |
| `#/firmware` | 模型与交付 | 配置、数据集、训练、交付、融合 |
| `#/knowledge` | 知识库 | 资料、索引及检索图表 |
| `#/archive` | 报告归档 | 清单检查与HTML导出 |
| `#/present` | 展示窗口 | 当前主要显示全国地图与统计 |

`#/login`用于角色进入验证，未计入9个业务页面。管理员控制台是后续需求，不得因旧文档存在`#/console`就判定当前已实现。

## 3. 本轮DOM采样

下面仅统计`p、button、td、label`的计算字号，以及根文档滚动尺寸。它不包含所有标题、SVG、Canvas内文字，不能与旧版“全页字号层级”直接比较，也不能据此判断首屏内容是否被内滚动隐藏。

| 路由 | 视口 | 根滚动宽度 | 采样字号 |
|---|---|---|---|
| `#/` | 1920×1080 | 1920px | 12px, 13px, 11px, 12.5px |
| `#/orders` | 1920×1080 | 1920px | 12px, 13px, 12.5px, 11px |
| `#/mapping` | 1920×1080 | 1920px | 12px, 13px, 11px |
| `#/twin` | 1920×1080 | 1920px | 12px, 13px, 11px |
| `#/hardware` | 1920×1080 | 1920px | 12px, 13px, 11px, 12.5px |
| `#/firmware` | 1920×1080 | 1920px | 12px, 13px |
| `#/knowledge` | 1920×1080 | 1920px | 12px, 13px, 11px |
| `#/archive` | 1920×1080 | 1920px | 12px, 13px |
| `#/present` | 1920×1080 | 1920px | 13px |

1920×1080根文档未出现横向溢出，不等于所有内部区域、弹窗和小屏均通过。11—13px样本字号说明常用阅读和操作文字仍偏小，应按v1.1重排后放大。

本次没有重新测量全页颜色数、边框数和逐项对比度。不得填写新的下降比例，或把旧版颜色统计挪作新版实测数据。

## 4. 页面视觉基线

| 页面 | 已观察情况 | 下一轮对照目标 |
|---|---|---|
| 总览 | 地图风格可保留；周边小字、部分摘要卡留白失衡 | 全国与项目作业分层，摘要收拢 |
| 工单 | 字段与日志密集，环境输入、版本差异和主动作不够集中 | 输入、校验结果、发布／接收就近排列 |
| 建图 | 主RViz区域使用静态素材；视频有占位 | 地图与视频能动态推进，任务状态独立清楚 |
| 孪生 | 新SOG可加载；室内工作台样例横向倾斜，未标定四柱 | 换比赛场景，修正方向、书签与热点关联 |
| 采集 | 大块黑色待数据区域，初始状态缺少阶段层次 | 测区、表面图和回波成为主视觉 |
| 模型与交付 | 版本字段横向过散，当前产物不够突出 | 当前产物与接收回验为主，历史版本次要 |
| 知识库 | 上半屏右侧大面积空置，检索与证据不在首屏中心 | 查询、回答和来源为主；管理与向量图分层 |
| 归档 | 长清单多、主要报告成果不突出 | 报告预览＋检查摘要＋异常修复 |
| 展示窗口 | 地图加载后可显示；没有呈现当前训练、采集等业务视图 | 根据viewType展示场景、曲线或报告 |

## 5. 交互基线

| 问题 | 已有证据 | 对视觉验收的影响 |
|---|---|---|
| 环境配置不共享且刷新恢复 | 沈显示CFG-05；独立史客户端与刷新后的沈显示CFG-02 | 成功反馈不能代表交接完成 |
| 交付下载与状态 | 点击未产生下载；切页后发布状态恢复 | 按钮反馈和真实结果不一致 |
| 巡检取消竞态 | 取消后延迟回调改回执行中 | 状态色和阶段文本不可信 |
| 小木错误匹配 | 无关问题返回场景；寒山寺查询返回示例寺数字 | 需合并查询范围、事实与工具结果 |
| 权限阻断协作 | 饶工单入口受阻，马样本／孪生入口受阻 | 需要区分页面读取与动作写权限 |

## 6. 新一轮采样要求

1. 固定提交号、未提交差异摘要、账号、演示会话、数据模式、路由与页签。
2. 固定1920×1080和1366×768，记录浏览器缩放、设备像素比、滚动位置与资源加载完成状态。
3. 每个关键任务采集初始、运行、完成、失败画面；记录主按钮和结果是否同屏。
4. 测量正文、按钮、表格和图表轴文字的实际字号与对比度；Canvas文字另查。
5. 记录内部滚动、遮挡、内容截断、地图比例、场景镜头和对比视角。
6. 对操作同时检查另一账号、刷新结果和展示窗口，不能只保存单页成功截图。
7. 原始统计与人工判断分开保存。未测的填“未测”，不以0或“无问题”代替。

## 7. 历史记录使用说明

下方保留原文件全文用于追溯。历史表格及其“达标”措辞仅表示旧规范与旧采样时点的结论，不代表当前v1.1通过。

历史文字中的“边框减少50%”“颜色总数上限”“全页最多5种字号”不再是独立验收条件。历史`28/16/13/12.5/11`也不作为新版目标。边框总数的变化不等于亮边框减少比例；颜色数量更不能替代对比度和布局检查。

---

## 附录 历史原文存档

# 视觉重构基线（重构前实测）

用 `node tools/shot.mjs --audit --url "http://localhost:5199/#/<route>" --out tmp-shot/a.png --wait 6500`
在 1920×1080 下逐页实测。本项目是 **hash 路由**，路由要写成 `#/orders`。

这张表是「颜色很乱、信息太多」的可量化证据，也是重构后的对照基准。

## 原始数据

| 路由 | textNodes | 字号数 | 字号集合 | 有色边框 | 文字色 | 背景色 | 边框色 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `#/` | 177 | **8** | 26/22/16/13/12.5/12/11/10 | 18/24 (75%) | 10 | 15 | 4 |
| `#/orders` | 216 | **9** | 26/16/14/13/12.5/12/11/10/9 | 34/41 (83%) | 9 | 13 | 4 |
| `#/mapping` | 114 | **8** | 26/16/14/13/12.5/12/11/10 | 28/29 (97%) | 10 | 15 | 5 |
| `#/twin` | 107 | **8** | 26/16/14/13/12.5/12/11/10 | 29/33 (88%) | 10 | 16 | 4 |
| `#/adapt` | 91 | **8** | 26/16/14/13/12.5/12/11/10 | 22/23 (96%) | 9 | 13 | 4 |
| `#/knowledge` | 89 | **7** | 26/16/14/13/12/11/10 | 13/16 (81%) | 9 | 13 | 4 |
| `#/archive` | 133 | **8** | 26/16/14/13/12.5/12/11/10 | 35/36 (97%) | 9 | 12 | 4 |
| `#/console` | 170 | **8** | 26/16/14/13/12.5/12/11/10 | 16/52 (31%) | 10 | 13 | 5 |

## 规范要求 vs 现状

| 规范条款 | 要求 | 现状 | 结论 |
| --- | --- | --- | --- |
| §2.2 字号 | 一个页面最多 **5** 个明显字号层级 | 每页 **7–9** 个 | ✗ |
| §10 P0 边框 | 砍掉约 **50%** 亮边框/装饰线 | 有色边框占全部边框 **75%–97%** | ✗ |
| §1.2 主色 | 只保留一个 UI 主色 `#4EA8FF` | 每页都在用 `rgba(48,97,219,.22)`（Demo2 旧主色 `#3061DB`） | ✗ |
| §1.4 文字色 | Text Primary = `#EAF3FF` | 首页仍有 26 处 `rgb(255,255,255)` 纯白 | ✗ |
| §4.6 状态标签 | 彩色圆点 + 文本 + 细边框，**不要整块高饱和填充** | 每页 topBg 里都有 `rgb(57,213,163)`（绿）与 `rgb(242,184,75)`（黄）成块填充 | ✗ |
| §9 Token | 禁止组件自行散落定义主题色 | 背景色每页 12–16 种 | ✗ |

## 全库共性问题（每一页都存在）

1. `rgba(48,97,219,0.22)` —— Demo2 遗留主色，必须清除
2. `rgb(57,213,163)` / `rgb(242,184,75)` 作为**块状背景**出现 —— 状态色被当作填充色用
3. 边框几乎全是蓝色系（`rgb(78,168,255)` 及其 alpha 变体）—— 缺少中性分隔
4. 字号在 10/11/12/12.5/13/14/16/22/26 之间无规律跳跃

## 重构目标

- 字号层级 ≤ 5（建议 26 KPI / 16 Panel 标题 / 13 正文 / 12.5 表格 / 11 辅助）
- 有色边框数量与占比显著下降
- `textColors ≤ 6`、`bgColors ≤ 8`、`borderColors ≤ 3`
- `topText` / `topBg` / `topBorder` 中不再出现 `rgb(255,255,255)` 与 `rgb(48,97,219)` 系列

---

## 附：边框总数前后对比（抽样实测）

| 路由 | 重构前边框数 | 重构后边框数 | 降幅 |
| --- | --- | --- | --- |
| `#/` | 24 | **3** | −87% |
| `#/orders` | 41 | **20** | −51% |
| `#/twin` | 33 | **11** | −67% |

规范 §10 P0 要求「砍掉约 50% 亮边框/装饰线」，三页全部达标或超出。
重构后剩余边框的有色占比虽然仍是 100%，但绝对数量已经很少，且全部收敛到
`rgba(78,168,255,.12~.18)` 这一档低对比 token。

## 附：字号集合（重构后）

九个路由全部落在规范 §2.2 定义的层级内：

`28 / 16 / 13 / 12.5 / 11`（Logo / Panel 标题 / 正文 / 表格 / 辅助），
`#/knowledge` 与 `#/present` 只用 4 级。

