# 木脉智检 UI 视觉素材 QA

检查日期：2026-09-13  
范围：桌面平台视觉资产与静态页面试装；不包含触屏前端。

## 自动检查

- 校验脚本结果：`PASS`，详见 `validation-results.json`。
- 数量：通用图标 33、业务图标 11、16px 简化图标 5、插图线性 SVG 2、透明 PNG/WebP 18；资源清单共 69 行资产记录。
- SVG XML 可解析；功能图标使用 `viewBox="0 0 24 24"`、`stroke="currentColor"`、`stroke-width="1.8"`、圆端点与圆连接。
- SVG 无脚本、无远程引用、无内嵌位图；颜色可由平台主题继承。
- 标准图标已做同墙视觉重量检查；复杂图标另提供 5 枚 16px 简化版。
- 正式插图 PNG/WebP 均保留透明通道；I01 低对比背景层的低 Alpha 为设计预期。
- WebP 网页副本保留与 PNG 母版一致的画布比例；本轮 WebP 均小于 100KB。

## 视觉检查

- P0 语义：设置、人工标记、服务离线、设备离线已经分离。
- P1 结构：样本分组不再像二维码；木柱不再像沙漏；模型导航不再与立方体/孪生混淆。
- 16px：expand、report、sample-group、multimodal、material-adapt 使用专用简化版，内部孔隙可见。
- 状态：默认、悬停、选中、禁用、成功、警告、错误、服务离线、设备离线、运行均有独立预览；禁用不冒充离线。
- 插图：小木保持中性单条状态灯；I03 黑色采集头在深色卡片上可辨；I05/I06 不含假文字、检测热区或伪造结果。

## 平台页面试装

- 已完成 1600×1000 静态试装：一级导航、设备与采集、数字孪生、模型适配、知识库与小木。
- 主次关系符合 PRD：设备轮廓、训练曲线、场景操作、回答与引用来源优先；插图为辅助。
- 平台导航保留中文标签和明确当前项；状态使用“图标 + 文本”，不只用颜色。
- 静态试装不等同于真实前端实现。仓库内未发现可接入的前端代码，键盘焦点、ARIA、真实交互和最终页面对比度仍需在代码接入后复验。

## 待补项

1. I02 巡检车缺少实物照片，当前文件明确标记 `CONCEPT-pending-photo`，不得作为设备实物一致性通过项。
2. 真实平台前端代码接入与角色任务走查待执行；建议至少验证“找设置、加人工标记、查看样本分组、打开模型适配”四项任务。

结论：视觉资产包与平台静态试装可进入验收；上述两项不得宣称已通过。
