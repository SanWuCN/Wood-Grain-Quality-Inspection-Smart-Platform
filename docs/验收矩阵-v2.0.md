# 小木智能体语音交互 v2.0 · 验收矩阵（AC-01~AC-06）

- 生成时间：2026-09-14T02:00:10（由 `汇总验收.py` 生成，勿手改）
- 判据来源：`docs/PRD-小木智能体语音交互-v2.0.md`（每条检查项后注明出处，无自创判据）
- 状态口径：`未验证` = 尚无验证方式或待人工；`通过`/`不通过` = 有脚本输出支撑；`证据不足` = 脚本不存在/跑失败/**跑通了但没断言这一条**
- 本矩阵只登记证据，不替产品下结论：**没有被断言过的项，一律不算通过**。

## 状态汇总

| 验收标准 | 检查项 | 通过 | 不通过 | 证据不足 | 未验证 |
| --- | --- | --- | --- | --- | --- |
| AC-01 | 16 | 15 | 0 | 1 | 0 |
| AC-02 | 8 | 5 | 1 | 2 | 0 |
| AC-03 | 5 | 5 | 0 | 0 | 0 |
| AC-04 | 8 | 8 | 0 | 0 | 0 |
| AC-05 | 4 | 4 | 0 | 0 | 0 |
| AC-06 | 8 | 8 | 0 | 0 | 0 |
| §9 | 2 | 2 | 0 | 0 | 0 |
| **合计** | **51** | **47** | **1** | **3** | **0** |

## AC-01 指定天气链路

### A1-1a · 真人声音说「小木小木」能唤醒（走真实服务）

- **判据**：「使用用户真人声音说『小木小木』…1. 唤醒成功」——23 条真人正样本推给 /wake，必须出现 wake 事件
- **PRD 出处**：§8 AC-01 第 1 条 + §8 AC-02「冻结真人正样本集」
- **验证方式**：tools/验收唤醒P1.py（分组①，需 8770）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 通过：真人正样本经真实服务 /wake 出现 wake 事件：21/23；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A1-1b · 浏览器端完整唤醒链路可用（挂点→采集→WS→KWS→命令）

- **判据**：「使用用户真人声音说『小木小木』，停顿 0.3–1.5 秒，再说『查近三个月天气』」——整条链路必须端到端成立
- **PRD 出处**：§8 AC-01 引言 + §3.1 已有链路
- **验证方式**：验收唤醒浏览器.mjs（需 5173+8770+8780）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-browser] 通过：退出码=0；断言 14/14；日志 D:\平台\voice-module\recordings\reports\logs\wake-browser.log

### A1-1c · 流式字幕正确（实时转写文字与所说内容一致）

- **判据**：「1. 唤醒成功，流式字幕正确」——需逐字核对字幕文本，不能只看识别出字就判过
- **PRD 出处**：§8 AC-01 第 1 条 + §FR-07「用户流式字幕…识别定稿后原气泡就地更新」
- **验证方式**：验收录音链路.mjs（真人录音+TTS 命令喂假麦克风，逐字比对定稿字幕并验前缀增长，需 5173+8770+8780）
- **当前状态**：`通过`
- **证据位置**：
  - [audio-chain] 通过：[audio-chain] 真实录音（fake-mic-chain.wav）→ 唤醒 1 次 → 命令 1 次；逐轮命令文本=["查近三个月天气"]（注：循环音频里唤醒词尾部会被服务端定稿成一轮命令，故逐轮列出，取**含命令内容的那一轮**比对）；界面上该轮定稿字幕="查近三个月天气"，PRD 标准句="查近三个月天气"，逐字一致=true（去标点后比较；取值窗口=该轮命令交付后 1.2s 内最后一次字幕，避免被下一轮 partial 覆盖）；该轮页面自报命令文本="查近三个月天气"（原始转写="小木小木，查近三个月天气。"）；流式证据：观察到 1 次前缀增长（4→9→11→11→7 字），字幕元素始终只有 1 个（就地更新、不重复新增气泡）；状态序列 idle→listening→thinking→speaking→idle；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\audio-chain.log

### A1-1d · 唤醒后停顿 0.3–1.5s 再说命令不被吃掉开头

- **判据**：「停顿 0.3–1.5 秒 → 命令」；「唤醒前后的预滚缓冲不得吃掉命令开头」
- **PRD 出处**：§8 AC-01 引言 + §FR-03 第 2 条
- **验证方式**：人工（混音用例只固定 0.5s 间隔，未覆盖 0.3/1.5 边界）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 证据不足：分组③只用固定 0.5s 间隔，未覆盖 0.3–1.5s 边界，故本项仍需人工做边界验收；参考 3/3
  - [pause-boundary] 通过：[pause-boundary] 按 PRD 的两个端点各喂一遍真实录音（真人「小木小木」+ 可调停顿 + TTS 命令「查近三个月天气」）：停顿 0.3s（下界）→ 通过；第 1 轮命令 t=6398ms 文本="查近三个月天气"（原始 "小木小木，。查近三个月天气。"）；停顿 1.5s（上界）→ 通过；第 1 轮命令 t=7147ms 文本="查近三个月天气"（原始 "小木，小木，。查近三个月天气。"）。判据=定稿命令/字幕逐字等于标准命令（起点不被预滚缓冲吃掉、终点不被静音判尾吃掉）；口径：复用 验收录音链路.mjs 的同一套页面内打点与"读字幕前先停流"取证，本脚本只负责生成两端音频并汇总；中间值 0.6s 由 A1-1c 的常规链路覆盖，不在本项重复；退出码=0；断言 1/1；日志 D:\平台\voice-module\recordings\reports\logs\pause-boundary.log

### A1-2 · 最终命中 site_weather（不是别的意图、不是 fallback）

- **判据**：「2. 最终命中 site_weather」
- **PRD 出处**：§8 AC-01 第 2 条 + §FR-05 标准句「查近三个月天气」
- **验证方式**：验收唤醒P1.py 分组③ + 验收唤醒浏览器.mjs（需 8770/5173）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-browser] 通过：浏览器端「命令文字出现在页面上」等断言：退出码=0；断言 14/14；日志 D:\平台\voice-module\recordings\reports\logs\wake-browser.log
  - [wake-p1] 通过：分组③「真人唤醒词 + TTS 命令句」3/3 命中且命令文字正确；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A1-3 · 主回答与 §FR-05 **逐字**完全一致

- **判据**：「3. 主回答与 FR-05 完全一致」——逐字比对（含全角括号、·、℃，句末句号），不是「看起来对」
- **PRD 出处**：§8 AC-01 第 3 条 + §FR-05 主回答原文 + §12 第 8 条「必须逐字核对主回答」
- **验证方式**：tmp-fr05-probe.mjs §4/§7（12 次取样比对 JSON.stringify 实际串与目标串）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：composeReply/ask 主回答与 PRD §FR-05 目标串 JSON.stringify 逐字相等；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-4a · weatherRange 值正确

- **判据**：「4. 三个业务字段值正确」；`weatherRange` = `2026-06-11 至 2026-09-10（近三个月）`
- **PRD 出处**：§8 AC-01 第 4 条 + §FR-05 业务状态表第 1 行
- **验证方式**：tmp-fr05-probe.mjs §5（rows.weatherRange === 预期）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：PASS  weatherRange = "2026-06-11 至 2026-09-10（近三个月）"；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-4b · weatherSummary 值正确

- **判据**：「4. 三个业务字段值正确」；`weatherSummary` = `梅雨期累计降水 412mm，8 月 3 次连续降雨过程，平均相对湿度 78%，日温差最大 11.4℃`
- **PRD 出处**：§8 AC-01 第 4 条 + §FR-05 业务状态表第 2 行
- **验证方式**：tmp-fr05-probe.mjs §5（rows.weatherSummary === 预期）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：PASS  weatherSummary = "梅雨期累计降水 412mm，8 月 3 次连续降雨过程，平均相对湿度 78%，日温差最大 11.4℃"；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-4c · weatherSource 值正确

- **判据**：「4. 三个业务字段值正确」；`weatherSource` = `归档天气档案 · 非实时联网查询`
- **PRD 出处**：§8 AC-01 第 4 条 + §FR-05 业务状态表第 3 行
- **验证方式**：tmp-fr05-probe.mjs §5（rows.weatherSource === 预期）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：PASS  weatherSource = "归档天气档案 · 非实时联网查询"；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-5a · 资料引用标题与位置可解析且正确

- **判据**：「5. 资料标题和 `降水与湿度 · w-01` 可见、可打开」——标题 `示例寺近三个月归档天气档案`，位置 `降水与湿度 · w-01`
- **PRD 出处**：§8 AC-01 第 5 条 + §FR-05 资料引用段
- **验证方式**：tmp-fr05-probe.mjs §6/§7b（气泡引用首条命中该 title+locator）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：引用解析命中 title=「示例寺近三个月归档天气档案」locator=「降水与湿度 · w-01」；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-5b · 资料引用在界面上真实可见、点击可打开原文位置

- **判据**：「资料标题和 `降水与湿度 · w-01` 可见、可打开」；「点击后打开现有知识库原文位置或至少打开对应知识文档」
- **PRD 出处**：§8 AC-01 第 5 条 + §FR-05 资料引用段第 3 条
- **验证方式**：验收-视觉矩阵.mjs（展开引用→点开→断言 hash 带 doc/chunk 且落点页「原文定位」已定位该块，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [visual-matrix] 通过：[visual-matrix] 引用区可见：标题「示例寺近三个月归档天气档案」定位符「降水与湿度 · w-01」（元素 BUTTON，键盘可达=true，同轮共 1 条引用）；点击后 hash #/ → #/knowledge?doc=doc-weather&chunk=w-01（带原文位置深链 chunk=w-01=true）；落点页「原文定位」面板=true、定位到该块=true（已定位=true、高亮块=["doc-weather#w-01"]、原文位置显示「原文位置 降水与湿度 · doc-weather#w-01」）；气泡内原文片段展开=true；截图 shots/citation/sources-expanded.png、shots/citation/after-open-source.png；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\visual-matrix.log

### A1-6 · 时间戳为本轮**实际**时间（HH:mm:ss），`19:03:48` 只是示例不得写死

- **判据**：「6. 时间戳为本轮实际时间」；「气泡底部显示本轮实际生成时间，格式 `HH:mm:ss`；`19:03:48` 只是示例，不得写死」
- **PRD 出处**：§8 AC-01 第 6 条 + §FR-05 时间戳段
- **验证方式**：tmp-fr05-probe.mjs §7b（读气泡视图 at）+ 工装做格式与实际时间窗校验
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：at=01:59:18；格式 HH:mm:ss 且与本次运行时刻相差 53s；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A1-7 · 全程不出现全屏遮罩

- **判据**：「7. 全程不出现全屏遮罩」；「默认入口固定在右下角，不显示全屏遮罩，不阻塞主业务页面」
- **PRD 出处**：§8 AC-01 第 7 条 + §FR-06 第 1 条
- **验证方式**：验收唤醒浏览器.mjs（浏览器端验收在无全屏遮罩前提下跑通）+ 人工目视
- **当前状态**：`通过`
- **证据位置**：
  - [wake-browser] 通过：该验收在页面无全屏遮罩的默认形态下跑通：退出码=0；断言 14/14；日志 D:\平台\voice-module\recordings\reports\logs\wake-browser.log

### A1-8 · 用户提供的该句验收样本 100% 通过

- **判据**：「用户提供的该句验收样本必须 100% 通过」
- **PRD 出处**：§8 AC-01 末段
- **验证方式**：工装只读核对 evaluation-v2.json 的 weatherSamples（仓库内是否存在用户真人说完整句的样本）
- **当前状态**：`证据不足`
- **证据位置**：
  - [eval-report-readonly] 证据不足：仓库内没有「用户真人说完整验收句（小木小木 + 查近三个月天气）」的样本：command.weatherSamples.total=0（recordings/ 下没有『查近三个月天气』的真人样本（见 gaps））；用户交付的真人素材只有唤醒词一类（wake.positive 23 条，canonical=「小木小木」，其中 21/23 唤醒成功 = FRR 8.70%）。整句样本缺失 ⇒ 分母为 0，无法判「100% 通过」；已能测的那一半（唤醒）在用户真人声音上是 21/23，本身也不是 100%。要给出本项结论需用户补一句完整录音。

### A1-9 · 未参与调参的冻结测试集命令识别正确率 ≥95%

- **判据**：「另行冻结的未参与调参测试集命令识别正确率目标不低于 95%」
- **PRD 出处**：§8 AC-01 末段
- **验证方式**：验收-命令准确率.mjs（清单冻结样本 → 8770 /transcribe?final=1 → 真实 matcher.understand()）
- **当前状态**：`通过`
- **证据位置**：
  - [command-accuracy] 通过：[command-accuracy] 冻结真人命令集 9/9 = 100.0%（目标 ≥95%）；转写逐字一致 9/9 = 100.0%；样本来自 datasetSplits 的 frozen-validation 会话（标定集 total=0）；清单声明 datasetSplit=["test"]；退出码=0；断言 1/1；日志 D:\平台\voice-module\recordings\reports\logs\command-accuracy.log

### A1-10 · TTS 只播报主回答一次，不逐项朗读字段名/来源/时间戳

- **判据**：「TTS 只播报主回答，不逐项朗读字段名、来源定位和时间戳」
- **PRD 出处**：§FR-05 TTS 段（§8 AC-01 第 3 条要求主回答与 FR-05 完全一致，含此约束）
- **验证方式**：tmp-fr05-probe.mjs §7d（speak() 调用次数与播报内容逐字比对）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过：speak() 调用次数 = 1（期望 1）；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log


## AC-02 唤醒准确率

### A2-1 · 冻结真人正样本集上 FRR <3%

- **判据**：「冻结真人正样本集上的漏唤醒率 FRR 目标 <3%」——23 条正样本漏 ≤0 条才满足（1/23=4.3%）
- **PRD 出处**：§8 AC-02 第 1 条
- **验证方式**：tools/验收唤醒P1.py 分组①（需 8770）
- **当前状态**：`不通过`
- **证据位置**：
  - [wake-p1] 不通过：分组①真人正样本命中 21/23，FRR=8.70%（目标 <3%）；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A2-2 · 真人负样本 FAR <1 次/小时

- **判据**：「真人负样本连续回放和现场底噪测试的误唤醒率 FAR 目标 <1 次/小时」；现有真人负样本为 0
- **PRD 出处**：§8 AC-02 第 2 条 + §3.2「当前真人负样本为 0，无法测量真实 FAR」
- **验证方式**：工装只读核对 evaluation-v2.json 的 wake.negativeReal（0 条 ⇒ 不可测；TTS 合成音不得冒充）
- **当前状态**：`证据不足`
- **证据位置**：
  - [eval-report-readonly] 证据不足：真人负样本 0 条 ⇒ 真实 FAR（次/小时）无分母、不可测：真人负样本 0 条：真实 FAR（次/小时）无法测量，AC-02 的 FAR 项只能标记为未测。；另有合成负样本 13 条 / 曝光 21.68s / 误唤醒 0 次，只能作下限参考，不得当 FAR。

### A2-2b · 参考数字：TTS 近音句/命令句负样本组零误唤醒（仅下限参考）

- **判据**：「真人负样本…误唤醒率」——TTS 合成音与真人差别大，只能作下限参考，不得替代 A2-2
- **PRD 出处**：§8 AC-02 第 2 条 + tools/验收唤醒P1.py 文件头「TTS 是拼接式合成音…只能当下限参考」
- **验证方式**：tools/验收唤醒P1.py 分组②（需 8770）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 通过：TTS 负样本组零误唤醒：13/13 条未唤醒（**仅下限参考，不得替代真人 FAR**）；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A2-3a · 现有 23 条真人正样本全部进入报告

- **判据**：「现有 23 条正样本必须全部进入报告」——报告里必须能看到 23 条逐条结果
- **PRD 出处**：§8 AC-02 第 3 条
- **验证方式**：tools/验收唤醒P1.py 分组① 逐条输出（23 行）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 通过：报告里出现的分组①逐条结果数 = 23（PRD 要求现有 23 条全部进入报告）；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A2-3b · 23 条不得同时既当调参集又当验收集（划分冻结）

- **判据**：「但不能全部同时作为调参集和验收集」；「训练/标定集与验收集在调参前固定」
- **PRD 出处**：§8 AC-02 第 3 条 + §FR-01 第 2 条
- **验证方式**：读 recordings/manifests/voice-intents-v2.json 的 datasetSplits（只读，不修改清单）
- **当前状态**：`证据不足`
- **证据位置**：
  - [manifest-readonly] 证据不足：清单 datasetSplits 结构核对：frozenAt=2026-09-13，冻结角色=['frozen-validation']，留出（frozen-validation）会话=['s-raw02', 's-intents-intro']（32 条），标定集 total=0，标定∩验收集=空集，全部 assignment=['frozen-validation', 'frozen-validation', 'synthetic-reference']；划分结构本身满足（标定集空 ⇒ 与验收集无交集），但清单已披露历史污染：tools/实验-两遍网格.py、tools/实验-两遍加复核.py、tools/测唤醒模型.py 等历史实验都直接跑在这 23 条上，现行服务默认参数（keywords-threshold=0.05 / keywords-score=2.…故「23 条从未同时充当调参集与验收集」在历史上不成立，需先补齐标定集（调参前必须先补采真人「小木小木」（建议 ≥20 条、换一次录音会话），作为标定集；在标定集到位之前，KWS 阈值沿用既有…）后重测才能给出干净结论。来源 voice-intents-v2.json（只读，未修改）

### A2-4a · 单次「小木」不唤醒

- **判据**：「『小木』单次…不得唤醒」；「只说一次『小木』不得唤醒」
- **PRD 出处**：§8 AC-02 第 4 条 + §FR-02 第 2 条
- **验证方式**：tools/验收唤醒P1.py 分组②（neg-xiaomu-once.wav，TTS 音）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 通过：分组②含 neg-xiaomu-once.wav 且整组零误唤醒（TTS 合成音，仅下限参考）；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A2-4b · 近音词（小木桥/小木屋/小木马/木木木/小幕/幕布/节目）不唤醒

- **判据**：「小木桥、小木屋、小木马、木木木…不得唤醒」
- **PRD 出处**：§8 AC-02 第 4 条 + §FR-01 负样本清单
- **验证方式**：tools/验收唤醒P1.py 分组②（TTS 音）+ 真人近音样本人工
- **当前状态**：`通过`
- **证据位置**：
  - [wake-p1] 通过：分组②含近音/命令句负样本 ['neg-xiaomuqiao', 'neg-xiaomuwu', 'neg-xiaomuma', 'neg-mumumu', 'neg-map-only', 'neg-history-only'] 且整组零误唤醒（TTS 合成音；真人近音样本仍需人工）；日志 D:\平台\voice-module\recordings\reports\logs\wake-p1.log

### A2-4c · 平台 TTS 回环不唤醒（自听抑制）

- **判据**：「平台 TTS 回环不得唤醒」；「不得因为小木自己的 TTS 播报、页面视频声音或音箱回环再次唤醒」
- **PRD 出处**：§8 AC-02 第 4 条 + §FR-02 第 4 条
- **验证方式**：验收录音链路.mjs（机制级：假麦克风循环播唤醒词，播报窗口内唤醒计数不增长 + suspend/resume 时序；物理回环未做）
- **当前状态**：`通过`
- **证据位置**：
  - [audio-chain] 通过：[audio-chain] 机制级实测（**无物理音箱→麦克风回环**，headless 没有声卡，这一点如实说明）：命令交付后推流被暂停（轮询在 t≈6720ms 记录到 suspended=true）=true；暂停窗口内唤醒计数变化=0（整段 1 → 1，未增加=true）⇒ 播报期间没有因自听再次唤醒；暂停期结束后自动恢复聆听 suspended=false（=true，对应 RESUME_AFTER_COMMAND_MS=2500ms，变迁序列 false→true→false）；结论口径：自听抑制链条（命令交付 → suspend → 定时 resume）成立；物理回环未做，故不是"音箱回环实测"；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\audio-chain.log


## AC-03 重复同句

### A3-1 · 连续两轮相同命令，两轮都返回（第二轮不因文本相同被吞）

- **判据**：「连续完成两轮相同天气命令，两轮均返回」；「不能只执行第一轮」
- **PRD 出处**：§8 AC-03 + §FR-09 第 1 条
- **验证方式**：复现关闭与重复命令.mjs（BUG-2 两轮气泡计数）
- **当前状态**：`通过`
- **证据位置**：
  - [close-repeat] 通过：「连续两次说同一句命令，第二次不执行」复现=否（当前行为正常）；连续派发两次相同的 open(question="介绍一下这套系统") 之后：用户气泡 2 个、小木气泡 2 个（期望各 2 个）；日志 D:\平台\voice-module\recordings\reports\logs\close-repeat.log

### A3-2 · 两轮 interactionId / 时间戳不同

- **判据**：「`interactionId` 和时间戳不同」；「每次命令必须携带唯一 `interactionId`」
- **PRD 出处**：§8 AC-03 + §FR-03 第 6 条 + §FR-09 第 2 条
- **验证方式**：验收-去重并发.mjs（两轮 interactionId/时间戳不等 + 同 id 只执行一次，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [dedup-concurrency] 通过：[dedup-concurrency] 两轮各自生成唯一 interactionId：第一轮 ["ia-mu03jxqp-1"]、第二轮 ["ia-mu03jymh-2"]，互不相同=true（读 .xd[data-interaction-id]，由 wakeChannel 每轮生成 / api 打开事件兜底生成）；两轮时间戳 "01:37:29" vs "01:37:30"（不同=true）；显式复用同一 interactionId 连发两次：第二次没有被执行=true（回答文本是否变化=false、期间新增回答次数=0；页面去重日志条数=1）；本轮页面 hash=；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\dedup-concurrency.log

### A3-3 · 两轮分别显示各自的时间戳（页面上可见）

- **判据**：「分别显示时间戳」
- **PRD 出处**：§8 AC-03 + §FR-09 第 1 条
- **验证方式**：验收-去重并发.mjs（读两轮 .xd__foot time 文本，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [dedup-concurrency] 通过：[dedup-concurrency] 每轮气泡底部都显示本轮自己的时间戳且页面上可见：第一轮 "01:37:29"（格式 HH:mm:ss=true）、第二轮 "01:37:30"（=true），两者不同=true，且都与当前墙钟 01:37:35 相差 ≤5 分钟=true；读取选择器 .xd__foot time（A1-6 已确认它绑定本轮 turn.at 而非渲染时刻）；口径：气泡区展示的是**当前轮**的时间戳（点形象可回看上一条回答），PRD 要求是"每轮各自显示"，不是"同时列出多轮"；截图 shots/dedup/two-rounds.png；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\dedup-concurrency.log

### A3-4 · 去重依据是 interactionId/eventId，不是命令文本

- **判据**：「去重仅能依据同一 `interactionId/eventId`，不得依据命令文本」
- **PRD 出处**：§FR-09 第 2 条 + §3.3 已确认问题第 3 条
- **验证方式**：代码走查 + 复现关闭与重复命令.mjs 的行为证据
- **当前状态**：`通过`
- **证据位置**：
  - [close-repeat] 通过：两轮同句是否都执行（去重不再按文本）的端到端行为证据：连续派发两次相同的 open(question="介绍一下这套系统") 之后：用户气泡 2 个、小木气泡 2 个（期望各 2 个）

### A3-5 · 同一时刻只处理一个语音命令（排队/打断/提示忙碌，不并发写 store）

- **判据**：「新命令到来时应排队、明确打断或提示忙碌，不得并发写入同一个 Agent store」
- **PRD 出处**：§FR-09 第 3 条（§8 AC-03 的并发面）
- **验证方式**：验收-去重并发.mjs（3 连发 + 6 连发，执行区间不重叠，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [dedup-concurrency] 通过：[dedup-concurrency] 100ms 内连发 3 条（介绍 → 天气 → 介绍）：状态时间线 thinking→speaking→thinking→speaking→thinking→speaking→idle；串行化纪律=**排队**（气泡用 Promise 链把命令按到达顺序串起来，见 XiaomuDock 的 askQueue）：回答依次出现 3 次且时间单调递增=true（10830→11192→11569），页面上当前回答始终只有 1 条=true（无两条并存/交错覆盖）；执行的 interactionId 数=0（页面自身日志行）；末条回答="示例寺 · 第二轮巡检（第二章）当前演示场景：示例寺 · 第"；截图 shots/dedup/burst3.png；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\dedup-concurrency.log


## AC-04 关闭

### A4-1 · standalone 路径：关闭按钮可退出

- **判据**：「standalone 和应用内挂载两条路径分别测试关闭按钮与 `Esc`」
- **PRD 出处**：§8 AC-04 第 1 条 + §FR-08 第 1 条
- **验证方式**：复现关闭与重复命令.mjs（BUG-1 记录：点了关闭按钮后界面是否仍在）
- **当前状态**：`通过`
- **证据位置**：
  - [close-repeat] 通过：「standalone 路径：点关闭按钮后界面不退出」复现=否（当前行为正常）；点了关闭按钮=true；900ms 后 .vc-root=false、standalone 容器=false；页面中心点命中的元素=CANVAS（在控制台内=false）；日志 D:\平台\voice-module\recordings\reports\logs\close-repeat.log

### A4-2 · 应用内挂载路径：关闭按钮可退出

- **判据**：「standalone 和应用内挂载两条路径分别测试关闭按钮与 `Esc`」
- **PRD 出处**：§8 AC-04 第 1 条
- **验证方式**：验收-关闭取消本轮.mjs（应用内路径 × 关闭按钮/Esc × 三种运行态，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [close-cancel-run] 通过：[close-cancel-run] 应用内挂载路径下 21 条断言通过 21 条；关闭入口（按钮/Esc/命令式）后 300ms 内面板消失、状态回 idle，2.6s 内不自回弹；本次共 21 条断言，失败项=[]；退出码=0；断言 21/21；日志 D:\平台\voice-module\recordings\reports\logs\close-cancel-run.log

### A4-3 · 关闭后 200ms 内界面消失、DOM 无残留遮罩

- **判据**：「关闭后 200ms 内界面消失，DOM 中无残留遮罩」
- **PRD 出处**：§8 AC-04 第 2 条 + §FR-08 第 5 条
- **验证方式**：tmp-ui-probe2.mjs「点关闭后 200ms / 1200ms 残留」（附加只读探针）
- **当前状态**：`通过`
- **证据位置**：
  - [ui-probe] 通过：关闭后 200ms 实测 {"vcRoot": false, "backdrop": false, "standalone": false}；日志 D:\平台\voice-module\recordings\reports\logs\ui-probe.log

### A4-3b · 关闭后不再拦截页面点击

- **判据**：「不再拦截页面点击」；「页面上不得留下透明遮罩或拦截点击的容器」
- **PRD 出处**：§8 AC-04 第 2 条 + §FR-08 第 5 条
- **验证方式**：验收-关闭取消本轮.mjs（两尺寸 elementFromPoint 采样 + 残留全屏层清点 + 真点页面导航，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [close-cancel-run] 通过：[close-cancel-run] 关闭后 1366×768 与 1920×1080 下均无残留全屏覆盖层（true）、页面 4 个采样点均不被小木命中（true）、真点页面导航生效 1/2 次（逐尺寸结果：1366x768「任务总览」路由  → #/；1920x1080「任务总览」路由未变（该元素可能不是导航））；注意：1920×1080 下点的是已选中项，路由不变属正常 —— 该判据只要求"关闭后点击能到达页面"至少一次成立，且不被遮挡；退出码=0；断言 21/21；日志 D:\平台\voice-module\recordings\reports\logs\close-cancel-run.log

### A4-4 · 正在进行的行为（播报/识别/待确认）关闭时正确停止或取消

- **判据**：「正在播报、正在识别和等待高风险确认时关闭，也必须正确停止/取消」；「关闭时立即停止当前 TTS…若有未确认高风险操作，应按『取消』处理」
- **PRD 出处**：§8 AC-04 第 3 条 + §FR-08 第 4 条
- **验证方式**：验收-关闭取消本轮.mjs（思考中/播报中关闭 + 关闭后 speak/cancel 计数，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [close-cancel-run] 通过：[close-cancel-run] 思考中（多步任务执行中）与播报中关闭均真正取消本轮：面板消失、不再有新播报（speak 计数不再增长）、无残留确认层；失败项=[]；退出码=0；断言 21/21；日志 D:\平台\voice-module\recordings\reports\logs\close-cancel-run.log

### A4-5 · 关闭气泡后再次喊「小木小木」仍能唤起

- **判据**：「关闭气泡后再次喊『小木小木』仍可正常唤起」
- **PRD 出处**：§8 AC-04 第 4 条 + §FR-08 第 5 条
- **验证方式**：验收唤醒浏览器.mjs（第 ④ 段：派发关闭事件后能否再次唤醒）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-browser] 通过：第 ④ 段「关闭控制台后能再次唤醒」：退出码=0；断言 14/14；日志 D:\平台\voice-module\recordings\reports\logs\wake-browser.log

### A4-6 · 所有关闭入口走同一个 closeAgent()，不留 Portal/监听/计时器/ASR/TTS 残留

- **判据**：「所有关闭入口必须调用同一个 `closeAgent()` 或等价的统一生命周期函数」；「standalone root、Portal、事件监听、计时器、ASR、TTS 和未完成确认必须正确清理」
- **PRD 出处**：§FR-08 第 2、3 条（§8 AC-04 的清理面）
- **验证方式**：验收-关闭取消本轮.mjs（4 入口同一 closeAgent + 20 次开关残留清点，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [close-cancel-run] 通过：[close-cancel-run] 开关 20 次后 面板=0 覆盖层=0 standalone=0 控制台根=0 播报中=false；关闭入口计数 {"button":1,"esc":2,"event":3}（按钮与命令式都派发同一事件）；退出码=0；断言 21/21；日志 D:\平台\voice-module\recordings\reports\logs\close-cancel-run.log

### A4-7 · 关闭气泡后常驻唤醒继续（只有显式关闭常驻唤醒才断链路）

- **判据**：「关闭气泡后常驻唤醒默认继续；只有用户点击『关闭常驻唤醒』才释放麦克风并断开 `/voice-wake`」
- **PRD 出处**：§FR-08 第 5 条（§8 AC-04 第 4 条的伴生约束）
- **验证方式**：验收唤醒浏览器.mjs（关闭后仍能再唤醒 = 链路未断）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-browser] 通过：关闭后仍可再唤醒 ⇒ 常驻链路未断：退出码=0；断言 14/14；日志 D:\平台\voice-module\recordings\reports\logs\wake-browser.log


## AC-05 UI

### A5-1a · 1366×768 下小木与气泡只占右下角局部区域

- **判据**：「1366×768、1920×1080 两种尺寸验收；小木与气泡只占右下角局部区域」；「气泡最大宽度 420px，在 1366×768 下不得遮挡超过约四分之一页面」
- **PRD 出处**：§8 AC-05 第 1 条 + §FR-06 第 3 条
- **验证方式**：tmp-ui-probe2.mjs（读出 panel 的 rect 与 areaRatio，只报实测数字）
- **当前状态**：`通过`
- **证据位置**：
  - [ui-probe] 通过：1366×768 下气泡面板 rect={
  "x": 904,
  "y": 61,
  "w": 420,
  "h": 407
 }，占视口面积 20.56%（PRD 上限约 1/4）；日志 D:\平台\voice-module\recordings\reports\logs\ui-probe.log

### A5-1b · 1920×1080 下小木与气泡只占右下角局部区域

- **判据**：「1366×768、1920×1080 两种尺寸验收」
- **PRD 出处**：§8 AC-05 第 1 条
- **验证方式**：验收-视觉矩阵.mjs（1366×768 / 1920×1080 面积与命中，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [visual-matrix] 通过：[visual-matrix] 1366×768 与 1920×1080 两尺寸实测：1366x768 下 .xd 占视口 21.2%、面板占 17.8%、右距 24px 下距 64px、页面中心 4 点被小木遮挡=false；1920x1080 下 .xd 占视口 13.2%、面板占 11.4%、右距 24px 下距 64px、页面中心 4 点被小木遮挡=false（判据：占比 ≤25%/20%，右距 ≤40px、下距 ≤90px，且中心区不被遮挡）；截图 shots/visual/1366x768-panel.png 等 4 张；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\visual-matrix.log

### A5-2 · 背景地图、表格和主要按钮仍可见、可点

- **判据**：「背景地图、表格和主要按钮仍可见、可点」
- **PRD 出处**：§8 AC-05 第 2 条 + §FR-06 第 1 条「不阻塞主业务页面」
- **验证方式**：验收-视觉矩阵.mjs（地图/表格/按钮可见性与真实点击，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [visual-matrix] 通过：[visual-matrix] 地图（总览页）两尺寸可见且中心点命中自身=true；表格（工单档案页）两尺寸可见、命中自身且行数>0=true；真实点击产生可观察效果 2/4 次：1366x768 地图(ov__map 1366×657 命中=true)、表格(4 行 命中=true)、按钮「SH-2026-0901上海市松江区」→ 路由 #/orders → #/orders?order=SH-2026-0901；1920x1080 地图(ov__map 1920×969 命中=true)、表格(4 行 命中=true)、按钮「SH-2026-0901上海市松江区」→ 路由 #/orders → #/orders?order=SH-2026-0901；截图 shots/visual/1366x768-orders-after-click.png 等 4 张；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\visual-matrix.log

### A5-3 · prefers-reduced-motion 下无持续动态，但状态信息完整

- **判据**：「`prefers-reduced-motion` 下无持续动态，但状态信息完整」；「尊重 prefers-reduced-motion，关闭动效后仍能通过文字和颜色辨认状态」
- **PRD 出处**：§8 AC-05 第 3 条 + §FR-06 第 6 条
- **验证方式**：验收-视觉矩阵.mjs（CDP 模拟 prefers-reduced-motion + 状态文字/颜色，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [visual-matrix] 通过：[visual-matrix] CDP 模拟 prefers-reduced-motion 生效=true；7 个部件的 animation/transition 全部 ≤0.01s 或 none（越界 0 个：[]）；状态信息仍完整：7 个状态徽标文字各不相同（待机/正在听/识别中/思考中/播报中/等确认/出错），状态配色组合 7/7 种不同（等确认=橙、出错=红，两者已分开）；截图 shots/visual/reduced-motion-1366x768-panel.png；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\visual-matrix.log


## AC-06 回归与安全

### A6-1 · 点击/文字/语音/COM4 对同一意图返回同一事实值

- **判据**：「点击、文字、语音、COM4 对同一意图返回同一事实值」；「同一命令通过语音、文字、示例按钮或 COM4 进入时，业务结果必须一致」
- **PRD 出处**：§8 AC-06 第 1 条 + §FR-04 第 2 条
- **验证方式**：tmp-fr05-probe.mjs（文字/语音两入口逐字一致）+ 批量意图测试.mjs（151 例）（点击/COM4 待补）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 证据不足：入口一致性未取得结论；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log
  - [entry-consistency] 通过：[entry-consistency] 四个入口各自读值并逐字节比对事实键值对：点击入口（示例说法按钮「查近三个月天气」）读 `.xm__facts` = 与文字入口一致 true；文字入口（输入框「查近三个月天气」）读 .xm__facts = [["weatherRange","2026-06-11 至 2026-09-10（近三个月）"],["weatherSource","归档天气档案 · 非实时联网查询"],["weatherSummary","梅雨期累计降水 412mm，8 月 3 次连续降雨过程，平均相对湿度 78%，日温差最大 11.4℃"]]；语音入口（气泡「查近三个月天气」）读 .xd__facts = 与文字入口一致 true；COM4 入口（串口映射表 example-history-summary 的 prompt「查今年五月示例寺巡检」经 openAgent 派发）读共享 store 的 bot 回合事实 = [["closed","3"],["followup","下一步：核对完工资料并安排人工验收（R04 Z04 柱脚渗水痕迹）。"],["open","3"],["reportedDone","4"],["total","6"]]，与同一句话走文字入口（读 .xm__facts）=[["closed","3"],["followup","下一步：核对完工资料并安排人工验收（R04 Z04 柱脚渗水痕迹）。"],["open","3"],["reportedDone","4"],["total","6"]] 一致=true；该回合在 store 里被直接观察到（控制台渲染的就是这份数据）；四处事实键值对完全相同=true；口径：点击/文字/语音三个入口读**各自界面真实渲染出来的事实表**（.xm__facts / .xd__facts），COM4 那一腿读控制台渲染所依据的同一份数据（store 回合 facts；本轮控制台 DOM 只渲染出旧回合，为避免读错回合改用数据源），不是读代码推断；截图 shots/entries/*.png；退出码=0；断言 1/1；日志 D:\平台\voice-module\recordings\reports\logs\entry-consistency.log

### A6-1b · COM4 串口入口与语音入口事实值一致

- **判据**：「同上：COM4 对同一意图返回同一事实值」
- **PRD 出处**：§8 AC-06 第 1 条 + §11 禁止另写一套执行器
- **验证方式**：验收-COM4一致.mjs（真实 parseSerialVoiceMap/matchSerialVoiceCommand/understand/composeReply + 派发点核对）
- **当前状态**：`通过`
- **证据位置**：
  - [com4-consistency] 通过：[com4-consistency] 映射表 3 条命令、解析错误 0 条、启用后帧匹配 3/3；mode=agent 的 2 条命令逐条跑真实 understand()+composeReply()：意图均明确、8 次取样事实恒定，其中 1 条与语音说法指向同一件事（意图与事实逐字节一致，同义措辞集合也一致）；派发点复用唯一入口 openAgent()（与文字/示例按钮同一符号），串口桥内无独立事实逻辑。本机未接 COM4 实物：映射表示例帧按设计 enabled:false，未做物理帧传输；本项证明的是入口与事实值同一，不是串口电气链路；退出码=0；断言 1/1；日志 D:\平台\voice-module\recordings\reports\logs\com4-consistency.log

### A6-2 · 风险等级 ≥3 的操作仍必须二次确认，未确认不执行

- **判据**：「风险等级 ≥3 的操作仍必须二次确认」；「高风险动作继续弹出明确的确认/取消气泡，未确认不得执行」
- **PRD 出处**：§8 AC-06 第 2 条 + §FR-04 第 4 条 + §2.2 非目标第 3 条
- **验证方式**：tmp-ui-probe2.mjs「高风险确认态」（只报 confirm 元素是否存在）+ 人工确认执行结果
- **当前状态**：`通过`
- **证据位置**：
  - [close-cancel-run] 通过：[close-cancel-run] 高风险多步任务触发后出现二次确认块（true，含危险按钮 true，文案「确认执行这条任务吗？（3 步） 前往 Z01 → 环绕采集一周 → 返回起点；步骤：首先前往 檐柱 Z01 → 环绕 Z01 采集一周 → 返回起点并上报结果 」）；不确认等 2.5s：仍停在确认态（true）、工具运行记录 done 增量=0 条、**带工具的执行步骤** done=0 个（都应为 0；另有两个规划步骤 done=2，属计划生成，不计入）；点「取消」后确认块消失（true）、取消态记录 0 条，回答文本「任务已下发：前往 Z01 → 环绕采集一周 → 返回起点。小车从 起点 / 殿门 出发，依次经过 起点 / 殿门 → Z」；退出码=0；断言 21/21；日志 D:\平台\voice-module\recordings\reports\logs\close-cancel-run.log
  - [ui-probe] 证据不足：高风险命令后出现确认元素=True，文案：✕；仍须人工确认「未确认不执行」；日志 D:\平台\voice-module\recordings\reports\logs\ui-probe.log

### A6-3a · 未命中意图不误执行工具（走 Fallback，不猜答案）

- **判据**：「未命中、低置信、服务断开、麦克风拒绝均不误执行工具」；「未命中意图：使用现有 Fallback，不猜答案」
- **PRD 出处**：§8 AC-06 第 3 条 + §FR-10 第 3 条
- **验证方式**：tmp-fr05-probe.mjs §7c（混淆句「今天天气怎么样」ask() 后 intentId 必须不是 site_weather）
- **当前状态**：`通过`
- **证据位置**：
  - [fr05-probe] 通过："混淆句 ask() 未命中 site_weather": true,；（「今天天气怎么样」ask() 后 intentId 不是 site_weather，回复走 Fallback）；日志 D:\平台\voice-module\recordings\reports\logs\fr05-probe.log

### A6-3b · 低置信命中不误执行高风险操作且标注不确定

- **判据**：「低置信命中…查询可回复但必须标注不确定，高风险操作禁止执行」
- **PRD 出处**：§8 AC-06 第 3 条 + §FR-10 第 2 条
- **验证方式**：验收-降级三态.mjs（低置信标注 + 高风险未执行，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [degrade-states] 通过：[degrade-states] 低置信说法 2 条，全部带不确定标注=2/2（附注/回答原文见上），全部未执行高风险动作=2/2（无确认块被跳过、回答里无"已执行"类措辞）：「把那个东西弄一下」→ 附注「可查询巡检资料、查看构件或启动当前业务流程；Top1 相似度 0.137 低于低」；「赶紧把那根柱子处理掉」→ 附注「可查询巡检资料、查看构件或启动当前业务流程；Top1 相似度 0.137 低于低」；截图 shots/degrade3/low-confidence.png；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\degrade-states.log

### A6-3c · 本地语音服务断开时进入错误态且不误执行工具

- **判据**：「本地语音服务断开：动态小木进入错误态并提供重连；文字入口和 COM4 入口仍可用」
- **PRD 出处**：§8 AC-06 第 3 条 + §FR-10 第 4 条 + §FR-02 第 6 条
- **验证方式**：验收-降级三态.mjs（浏览器侧隔离 8770/8780，不杀服务，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [degrade-states] 通过：[degrade-states] 浏览器侧隔离 8770/8780（未动真实进程、麦克风已授权）后：小木进入 state=error（徽标「出错了」），原因指向**链路断开**=true（通道 note「唤醒通道断开，正在重连…（2000ms 后第 3 次重试）」），不显示「正在听」（开关文案「开启常驻唤醒」）；同一状态下文字入口仍可用：主回答含 412mm（「归档天气档案（2026-06-11 至 2026-09-10…」）；COM4 入口不依赖本地语音服务（其挂载点见 Shell 的 SerialVoiceBridge，需硬件才能端到端，本项只证明语音服务断开不影响它）；截图 shots/degrade3/service-down.png；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\degrade-states.log

### A6-3d · 麦克风被拒绝时不显示假「正在听」，且不误执行工具

- **判据**：「服务不可用、麦克风被拒绝或 KWS 退化时必须显示真实状态，禁止显示『正在听』但实际没有音频链路」
- **PRD 出处**：§8 AC-06 第 3 条 + §FR-02 第 6 条
- **验证方式**：验收-降级三态.mjs（页面级注入 NotAllowedError，等价用户点「拒绝」；断言不得显示正在听，需 5173）
- **当前状态**：`通过`
- **证据位置**：
  - [degrade-states] 通过：[degrade-states] 页面级注入 getUserMedia → NotAllowedError（等价于用户点「拒绝」，与浏览器抛给页面的异常同名）后调用 start()：返回 true；通道 state=error、note「麦克风权限被拒绝，唤醒不可用」⇒ 权限拒绝已生效；界面 state=error（徽标「出错了」）、面板可见=true ⇒ 未显示假的「正在听」；提示文字 ["断开","不可用","不可用","重连","重连"]；工具产物：事实行 0、步骤 0，回答「本地语音服务当前不可用：麦克风权限被拒绝，唤醒不可用。文字入口和 COM4 语音串口入口不受影响，仍」；截图 shots/degrade3/mic-denied.png。口径说明：这是页面级模拟而非真实授权弹窗（headless 没有弹窗），但走的是产品处理权限失败的同一分支；退出码=0；断言 3/3；日志 D:\平台\voice-module\recordings\reports\logs\degrade-states.log

### A6-4 · /wake 通道本身自检通过（帧数/解码/预滚）

- **判据**：回归基线：语音链路健康，避免把链路故障误判成意图问题
- **PRD 出处**：§9 非功能要求「全链路优先本地运行」+ §3.1 已有链路（AC-06 回归前提）
- **验证方式**：tools/验证唤醒通道.py（需 8770）
- **当前状态**：`通过`
- **证据位置**：
  - [wake-channel] 通过：退出码=0；帧数：服务端 21 / 客户端发出 21；日志 D:\平台\voice-module\recordings\reports\logs\wake-channel.log


## §9 非功能要求（交付条件，非 AC 编号）

### N1 · 唤醒结束到可见「正在听」反馈 ≤800ms

- **判据**：「唤醒结束到可见『正在听』反馈目标 ≤800ms」
- **PRD 出处**：§9 非功能要求第 3 条
- **验证方式**：验收录音链路.mjs（真实录音喂麦 + 页面内 performance.now 打点、25ms 轮询，需 5173+8770+8780）
- **当前状态**：`通过`
- **证据位置**：
  - [audio-chain] 通过：[audio-chain] 唤醒判定结束 → 界面进入「正在听」= 0ms（目标 ≤800ms）；页面内 performance.now 打点、25ms 轮询分辨率：唤醒计数出现 t=3479.1ms、.xd[data-state=listening] t=3479.1ms；本轮共 1 次唤醒，取首次；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\audio-chain.log

### N2 · 命令结束到首个完整回复气泡 ≤2s（首次预热单独记录）

- **判据**：「命令结束到首个完整回复气泡目标 ≤2s（首次模型预热单独记录）」
- **PRD 出处**：§9 非功能要求第 4 条
- **验证方式**：验收录音链路.mjs（同上；要求气泡文本等于最终值才算「完整」，需 5173+8770+8780）
- **当前状态**：`通过`
- **证据位置**：
  - [audio-chain] 通过：[audio-chain] 命令判尾结束 → 首个**完整**回复气泡（文本等于最终值，127 字）= 179.6ms（目标 ≤2000ms）；25ms 轮询分辨率：命令计数出现 t=6720.4ms、完整回答 t=6900ms；服务已预热（本轮是第 1 次命令），首次加载预热时间不计入；回答首 40 字「归档天气档案（2026-06-11 至 2026-09-10（近三个月））：梅雨…」；退出码=0；断言 4/4；日志 D:\平台\voice-module\recordings\reports\logs\audio-chain.log

