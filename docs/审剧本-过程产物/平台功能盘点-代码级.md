# 木脉智检平台 · 代码级真实功能盘点

> 盘点方式：直接读源码 + 读运行中服务的真实响应（`http://127.0.0.1:8000`，2026-09-16 实测）。
> 所有结论均给 `文件:行号`。读不到的一律写「未找到」，不做推测。
> 覆盖范围：`src/pages/MumaiDashboard/**`、`server/**`。

---

## 0. 一句话结论（供剧本对照）

| 判定 | 内容 |
|---|---|
| **真能跑（有后端、有写库、有回执）** | 工单触发建单 / 指派 / 环境草稿与校验 / 下发扫描仪与回执、归档 SHA-256 校验与补传、知识库（资产 16.7 万条、检索、索引任务）、排练控制台（会话/快照/诊断包）、投屏焦点、平台主机资源（真实采样）、设备网关上行（终端 2s 上报）、小车建图巡航（14 个控制动作）、传感器 BLE 链路、3DGS 渲染（Spark + 真实 31.7MB `.sog`）、小木的语音（60 条预生成 mp3 + 浏览器 TTS）与流式 ASR |
| **只有界面，数据全来自前端种子/本地 state（刷新即丢）** | 训练验证（损失曲线/任务控制台/执行节点）、数据集清洗与划分、融合分析、更新交付的"待提交产物"、图像标注、异常排查日志、采集作业的批次与波形、设备启动检查签署、旧工单（演示回放那几张）的一切改动 |
| **平台根本没有** | 工单识别 / 读取附件（OCR）、录像回放、设备管理列表页、复巡计划的创建与下发、检测报告生成（只有"归档完整性报告"HTML）、模型量化的执行、3DGS 里的构件锚点标注（锚点字段恒为空、无标定 UI）、构件↔3D 场景的坐标关联（只有字符串匹配） |

---

## 1. 一级导航清单（8 + 2）

导航项定义：`src/pages/MumaiDashboard/design.ts:126-135`；路由表：`src/pages/MumaiDashboard/routes.tsx:42-52`；
外壳（顶栏/导航/状态条/小木）在 `Shell.tsx`，`AppShell.tsx:23-29` 只做 Provider 包裹。
导航按角色过滤（`Shell.tsx:225`、`auth.ts`），未登录先跳 `/login`（`routes.tsx:34-41`）。

| # | 中文名 | 路由 | 页面文件 |
|---|---|---|---|
| 1 | 任务总览 | `/` | `pages/Overview.tsx` |
| 2 | 工单档案 | `/orders` | `pages/Orders.tsx` |
| 3 | 建图巡航 | `/mapping` | `pages/Mapping.tsx` |
| 4 | 数字孪生 | `/twin` | `pages/Twin.tsx` |
| 5 | 硬件详情 | `/hardware` | `pages/Hardware.tsx` |
| 6 | 固件及模型 | `/firmware` | `pages/Firmware.tsx` |
| 7 | 知识库 | `/knowledge` | `pages/Knowledge.tsx` |
| 8 | 报告归档 | `/archive` | `pages/Archive.tsx` |
| — | 大屏展示（无外壳） | `/present` | `pages/Present.tsx`（`Shell.tsx:451-461` 单独渲染，不带导航） |
| — | 排练控制台 | `/console` | `pages/Console.tsx`（管理员权限才可见，`http.mjs:1126-1130`） |

### 1.1 任务总览 `/`（`pages/Overview.tsx`）
- 主视觉：3D 中国/上海地图铺满内容区（`Overview.tsx:1022-1024`，`mapDemo/index.tsx`，GeoJSON + 地表贴图 + 点位标记）。
- 四块浮层看板：
  - 左·**巡检概览**（`SituationPanel` `Overview.tsx:129-226`）：覆盖省份 / 古建点位 / 完成巡检 / 完成率四个 KPI + 点位状态环形图（ECharts，`OverviewCharts.tsx`）+ 图例。
  - 左·**设备状态**（`DevicePanel` `Overview.tsx:239-420`）：毫米波扫描仪 / 智能巡检车 / 算力服务器三行 + 四路通道横向条形图。
  - 右·**工单看板**（`RiskOrderPanel` `Overview.tsx:422-484`）：待处理/处理中/待验收三档计数 + 工单循环轮播列表（`OrderBoardList:500-600`）+ 「查看全部」弹窗（`OrderListModal:603-660`）。
  - 右·**平台数据**（`PlatformDataPanel` `Overview.tsx:662-994`）：存储/内存/GPU/功耗/网络五行 + 逐台负载图 + 存储构成图（数据来自后端主机真实采样）。
- 底部：面包屑「全国总览 / 上海市」+ 图例 + 「进入上海/返回全国」按钮（`Overview.tsx:1046-1101`）。
- 点位详情浮层：`map/SiteDetailCard.tsx`（点地图点位弹出，内含跳工单入口 `:226`）。

### 1.2 工单档案 `/orders`（`pages/Orders.tsx`）
- 工具栏：「生成工单」「确认草稿工单」（`:428-444`）+ 来源标记「演示回放」。
- 左栏两块：**工单列表**（新流程，服务端；四个筛选页签 全部/待指派/进行中/已归档 `:458-470`，搜索框 `:471-478`，列表 `:479-498`）、**历史工单**（演示回放旧单，可收起 `:501-525`）。
- 右栏：新工单走 `WorkOrderDetail.tsx`；旧工单走内联详情（`Orders.tsx:533-868`）：
  - 工单详情面板（KV + 暂停/归档按钮 `:534-610`）
  - 构件清单 Z01–Z04 表（`:574-589`）、任务附件列表（`:591-609`）
  - **环境记录与配置校验**（`:614-782`）：温度/湿度/风速/仪表位置读数 + 「录入读数」「运行校验」「全栈接收并返回 ack」+ 校验结论列表 + 提交状态/设备确认/配置版本
  - **环境补偿与配置差异**（`:785-829`）：补偿前后对照表 + 历史环境记录
  - **人员与操作记录**（`:837-867`）：成员卡片 + 最近一条操作 + 「查看全部」弹窗（`:873-909`）
- 弹窗：录入环境读数（`:919-987`）、建单（`WorkOrderCreate.tsx`）。

### 1.3 建图巡航 `/mapping`（`pages/Mapping.tsx`）
- 顶部两条横幅：只读监视（未配令牌）/ 状态不新鲜（`:458-473`）+ 操作失败提示（`:474-482`）。
- 左栏随模式切换（模式开关 `:494-509`，只有「建图 / 巡航」两个功能 `:56-59`）：
  - 建图：「开始建图 / 结束建图 / 重置地图」（`:548-570`）、保存名称输入 + 「保存地图 / 去巡航」（`:571-596`）、**已保存地图与路线**（`SavedMapsPanel:1048-1145`：预览图、载入、下载 map.pgm / map.yaml、重新读取）
  - 巡航：① **地图与定位**（`:612-670`：自动定位 / 手动发布位姿，地图点选 + 拖车头方向）；② **航点**（`:671-755`：航点列表、移动/删除、预览路径、清空、保存为路线）；③ **速度与下发**（`:756-839`：限速滑杆 + 下发限速 + 开始巡航/暂停/继续/停止 + 车况读数）
- 中栏：地图 / 屏幕画面 / 两者 三个页签（`:886-916`），栅格地图 Canvas（`cart/MapCanvas.tsx`）+ RViz MJPEG；右侧「相机流」（`:968-988`）。
- 底部：**小车参数**（`cart/ParameterStrip.tsx`，可折叠 `:990-1046`）+ 「原始数据」弹窗（`InspectorModal:1146-1212`：只读接口 health/session/config + 当前状态 + 清单）。

### 1.4 数字孪生 `/twin`（`pages/Twin.tsx`）
- 工具栏：工单下拉（`:246-255`）、「适应视图」（`:256-258`）、「上传模型文件 / 替换模型文件」（`:259-263`，仅全栈开发工程师）。
- 主视图（`SplatStage.tsx`，Spark + SplatMesh 渲染 `.sog`/`.spz`）+ 底部操作提示条（`:310-314`）；空态「未收到模型文件」（`:283-299`）、错误态「模型文件无法渲染 + 重试」（`:301-308`）。
- 右栏：**操作说明**（`WASD/QE/Ctrl/Shift` 键位表 `:319-332`）、**场景版本**（`:334-397`：场景行 + 「运行检查」「发布场景」+ 逐项检查结论）、**热点详情**（`:399-429`：构件/回波/融合规则 + 「查看完整证据」弹窗 `:432-596`：原图、回波波形 `WaveChart`、初筛、融合结果、历史任务）。
- 「上传高斯模型」弹窗（`:612-694`）：文件选择 → 上传 → 绑定工单。

### 1.5 硬件详情 `/hardware`（`pages/Hardware.tsx`）
- 三个页签（`:58-62`）：**采集作业** / **异常排查** / **硬件监看**（toolbar 页签按钮 `:933-938`，`?tab=` 写 URL）。
- 采集作业（`CaptureRun.tsx`）：命令条（批次下拉、配置/模型版本、启动检查进度、启动/暂停采集 `:166-172`）+ 扫描枪姿态与采集端屏幕工作区（`SensorWorkspace.tsx`，实时 BLE 数据）+ 采集数据三个页签（接收进度 / 波形与标记 / 参考样本 `:174-181`）+ 「设备启动检查」弹窗（逐条签署后才可开始 `:65-131`）。
- 异常排查（`TriageLog.tsx`）：异常事件列表（`:466-523`，点开详情弹窗）+ 设备日志（日志包列表 + 来源/设备/结果筛选 `:524-624`）。
- 硬件监看（`MonitorTab:851-874`）六块：**设备状态**（`:253-450`：设备/编号/来源/连接/任务/版本 + 「请求状态快照」「请求上传批次」+ 读数 + 「现场调参」弹窗）、**设备读数**（`:452-504`）、**通道状态**（`:506-553`）、**采集接收**（`:555-677`：批次三路实收/应收进度 + 「补传」）、**终端遥测**（`:691-785`）、**设备预览**（`:787-849`：设备推流单帧）。

### 1.6 固件及模型 `/firmware`（`pages/Firmware.tsx`）
- 页签（`:48-54`）：**全局配置** / **数据集** / **训练验证** / **更新交付** / **融合分析**。
- 全局配置：**版本管理**摘要表（`:336-415`：8 个组件 × 70+ 发行记录，按硬件/算法/平台分组，含设备回报比对 `reportState:96-120`）+ 版本详情弹窗（`:190-260`，可挑版本回退）+ **待生效改动**面板（`:416-445`）。
- 数据集（`adaptTabs.tsx` `DatasetTab:56-`）：清洗流程（`DatasetCleanFlow.tsx`）、本轮版本表、样本清单表、**分组检查**（训练集/验证集/测试集）。
- 训练验证（`TrainingRun.tsx`）：训练任务卡 + 配置弹窗、任务控制台（日志回放）、执行节点占用、**损失曲线**（训练/验证/基线）、**独立测试集对比**（新旧分/召回/漏检误报/按材种回归/逐样本对比/验收规则）、采集数据包表 + 导入数据包弹窗。
- 更新交付（`DeliveryCenter.tsx`）：本轮产物、待提交产物 + 上传、提交前校验、已发布产物（下载/回验/记录烧录）。
- 融合分析（`adaptTabs.tsx` `FusionTab:348-`）：数据完整性、分支状态、**图像标注**表、测区匹配、雷达特征、融合规则与结果。

### 1.7 知识库 `/knowledge`（`pages/Knowledge.tsx`）
- 四个页签（`:47-52`）：**总览** / **数据资产** / **RAG 索引** / **检索验证**（写 URL）。
- 工具栏：「数据说明」抽屉（`:301`，含夹具报告）、「导入资料」抽屉（`:302`）、资产/索引/检索操作按钮（`:305-`）。
- 视图：`knowledge/views/OverviewView.tsx`（数据资产库/RAG 索引库/更新任务三块卡片 + 覆盖分布）、`AssetsView.tsx`（资产列表 + 筛选 + 分页 + 批量操作）、`IndexesView.tsx`（索引服务状态/覆盖分布/更新任务/版本记录/索引配置表单）、`SearchView.tsx`（检索输入 + 工作场景示例 + 结果列表 + 证据详情 + 「查看来源资产」「在图谱中定位」）。
- 图谱：`knowledge/RelationGraph.tsx`（业务/血缘两种视图，可全量加载）。
- 抽屉：资产详情 `AssetDrawer.tsx`、任务详情 `JobDetailDrawer.tsx`、导入 `ImportDrawer.tsx`。
- 引用深链：`?doc=&chunk=` 落到原文位置（`Knowledge.tsx:64-142`）。

### 1.8 报告归档 `/archive`（`pages/Archive.tsx`）
- 工具栏：「运行交付文件校验」（`:189-192`）、「下载校验报告」（`:193-195`，HTML）、打印版/PDF（`:200`）。
- **交付清单**面板（`:221-273`，按组展示 24 项 + 全部明细弹窗 `:394-470`）。
- **校验结果**面板（`:275-356`）：总数/通过/缺失/不一致 + 逐项结论。
- **归档范围与更新包**面板（`:358-`）。
- 补传 / 重选副本：只在有问题项上出现（`:448-455`）。

### 1.9 大屏展示 `/present`（`pages/Present.tsx`）
按 `focus.viewType` 渲染七种视图（`:1-21`、`focus.ts:28-46`）：`map` 全国地图（`:62-77`）、`scene` 数字孪生（`:79-124`）、`capture` 采集作业（`:125-189`）、`training` 训练验证（`:190-276`）、`delivery` 更新交付（`:277-345`）、`report` 报告归档（`:346-407`）、`workspace` 工作区兜底（`:408-457`）。展示窗口隐藏操作控件，只读展示。

### 1.10 排练控制台 `/console`（`pages/Console.tsx`）
- 工具栏：「新建演示会话」「导出诊断包」「重新读取」（`:115-123`）。
- 三块：**演示会话**（`:146-160`，列出全部会话与实体/事件数）、**阶段快照**（`:162-213`，选阶段+备注→「捕获当前状态」，每条快照可「恢复 / 删除」）、**启动预检**（`:215-`）。

---

## 2. 每页关键交互动作（点了会发生什么）

> 标注格式：`动作` → `去向`（真实接口 / 前端本地）。

### 2.1 总览 `/`
| 动作 | 结果 |
|---|---|
| 点「进入上海 / 返回全国」 | 只改 `map/store.ts` 的 mode，播镜头推进动画（`Overview.tsx:1085-1100`）；本地 |
| 点地图点位 | `map/store.ts` 选中 → `SiteDetailCard` 浮层；本地 |
| 点工单行 / 「查看全部」里的行 | `navigate('/orders?order=<id>')`（`Overview.tsx:585`、`:637`） |
| 点设备状态前两行 | 跳 `/hardware?tab=monitor` / `/mapping`（`Overview.tsx:278`、`:288`） |
| 点第三行（算力服务器） | 打开 `ResourceModal`（`:252-255`、`:989-991`），数据 2s 轮询 `GET /api/platform/resources` |
| 「平台数据」面板 | 2s 轮询 `GET /api/platform/resources` + `GET /api/platform/resources/history`（`usePlatformResources.ts`） |

### 2.2 工单 `/orders`
| 动作 | 结果 |
|---|---|
| 筛选页签 / 搜索 | 只写 store，300ms 防抖后 `GET /api/work-orders?filter=&q=`（`Orders.tsx:176-179`、`store/workOrders.ts:73`） |
| 点列表行 | 写 URL `?order=`，`GET /api/work-orders/{id}`（`Orders.tsx:204-216`） |
| 「指派人员 / 调整指派」 | 选负责人 + 参与人 + 职责 → `PUT /api/work-orders/{id}/assignment`（`orders/AssignmentPanel.tsx:147`、`Orders.tsx:242-248`） |
| 「录入读数」（新单） | `PUT /api/work-orders/{id}/environment-draft`（`EnvironmentPanel.tsx:170-181`、`Orders.tsx:249-252`） |
| 「运行校验」（新单） | `POST /api/work-orders/{id}/environment/validate` → 生成不可变配置版本（`Orders.tsx:253-258`） |
| 「下发扫描仪」 | `POST /api/work-orders/{id}/dispatches`，带幂等 key（`orders/DispatchPanel.tsx:146-156`、`Orders.tsx:259-264`） |
| 「开始作业/提交验收/验收通过并归档/暂停/恢复/归档」 | `POST /api/work-orders/{id}/status`（`WorkOrderDetail.tsx:35-42,128-137`） |
| 「删除工单」 | 二次确认 → `DELETE /api/work-orders/{id}`（`WorkOrderDetail.tsx:257-284`） |
| 「生成工单」 | `WorkOrderCreateModal` 表单 → 写 `context.tsx` 的 `orders` **本地 state**（`context.tsx:241-265`），刷新丢失 |
| 「确认草稿工单」 | 本地把 `DRAFT_ORDER` 状态改成「待复核」（`context.tsx:225-232`） |
| 旧单「暂停/归档」 | 本地 `setOrderStatus`（`Orders.tsx:543-558`、`context.tsx:267-269`） |
| 旧单「运行校验」 | `POST /api/commands` `action=environment.publish`（`Orders.tsx:352-366`） |
| 旧单「全栈接收并返回 ack」 | `POST /api/commands` `action=environment.receive`（`Orders.tsx:396-401`） |
| 全局隐藏快捷键 `Ctrl+Q+L` | `POST /api/work-orders/trigger`（`useWorkOrderShortcut.ts:104-106`、`store/workOrders.ts:112`），幂等、失败重试 5 次 |

### 2.3 建图巡航 `/mapping`（全部经 `POST /api/cart/action`，服务端持令牌转发小车）
`开始建图`→`mapping/start`；`结束建图`→`mapping/stop`；`重置地图`→`mapping/restart`；`保存地图`→`mapping/save`；`载入`→`navigation/load`；`自动定位`→`navigation/auto-localize`；`发布位姿`→`navigation/localize`；`预览路径`→`navigation/preview`；`开始巡航`→`navigation/start`；`暂停`→`navigation/pause`；`继续`→`navigation/resume`；`停止`→`control/stop`；`下发限速`→`navigation/speed`；`保存为路线`→`routes/save`。
（动作名与权限表：`server/services/cart.mjs:81-95`；页面实现：`Mapping.tsx:264-352`。）

### 2.4 数字孪生 `/twin`
| 动作 | 结果 |
|---|---|
| 选工单 | 写 URL `?order=`，从共享快照里筛该工单的 scene 实体（`Twin.tsx:130-139`） |
| 「上传模型文件」 | `POST /api/files?dir=scenes`（真字节）→ `POST /api/commands` `scene.submit` 绑定工单（`Twin.tsx:644-659`） |
| 「运行检查」 | `POST /api/commands` `scene.check` → 三项检查（资源/锚点/书签，`workflow.mjs:353-377`） |
| 「发布场景」 | `POST /api/commands` `scene.publish`，须检查通过（`Twin.tsx:184-201`、`workflow.mjs:379-399`） |
| 「查看完整证据」 | 打开弹窗，内容来自 seed（`HOTSPOTS/CURRENT_RISKS/WAVEFORMS/HISTORY_RISKS`，`Twin.tsx:203-232,432-596`） |

### 2.5 硬件详情 `/hardware`
| 动作 | 结果 |
|---|---|
| 页签切换 | 写 URL `?tab=`，保留 `?batch=`（`Hardware.tsx:885-891`） |
| 「请求状态快照」/「请求上传批次」 | `POST /api/devices/{id}/commands` `{type:query_status|request_upload}`（`Hardware.tsx:260-270`、`:437-440`） |
| 「补传」 | 在线时 `POST /api/devices/{id}/commands`；**离线时只弹 toast、不发请求**（`Hardware.tsx:584-598`） |
| 采集作业「启动采集」 | 弹设备启动检查 → 逐条签署 → 改本地 `phase`（`CaptureRun.tsx:171,182`） |
| 扫描枪「扫描 / 连接 / 断开 / 陀螺校准」 | `POST /api/sensors/{scan|connect|disconnect|gyro-calibrate}`（`SensorWorkspace.tsx:224-235`） |
| 异常排查点事件行 | 打开详情弹窗，纯本地筛选（`TriageLog.tsx`） |

### 2.6 固件及模型 `/firmware`
| 动作 | 结果 |
|---|---|
| 版本详情里「选这个版本」 | 只改页面 `draft` state（`Firmware.tsx:304-`），**不写后端、不下发** |
| 「确认改动/清空改动」 | 本地 state（`Firmware.tsx:383-404`） |
| 数据集清洗每一步 | 本地 `cleanLogic.ts` 规则现算（`DatasetCleanFlow.tsx`，`adaptTabs.tsx:127` 回填版本号到 state） |
| 分组「编入所选集合 / 整组调整后重跑」 | 本地 `useState` 改划分，`lib.checkGrouping` 现算冲突（`adaptTabs.tsx:87-118`） |
| 训练「配置并提交」 | 本地定时器逐行回放日志（`TrainingRun.tsx:74-77`），**零后端调用** |
| 更新交付「提交」 | 只改本地 `artifacts` state（`DeliveryCenter.tsx:249-265`） |
| 更新交付「下载」 | 真实 `GET /api/files/{id}/download`（`DeliveryCenter.tsx:276-298`） |
| 更新交付「回验」 | 读回文件算 SHA-256 → `POST /api/commands` `artifact.receipt`（`DeliveryCenter.tsx:307-330`） |

### 2.7 知识库 `/knowledge`
读：`GET /api/knowledge/{overview,assets,assets/:id,graph,jobs,jobs/:id}`；检索：`POST /api/knowledge/search`。
写：全部走 `POST /api/commands`，动作包括 `asset.register / asset.revise / asset.setInclusion / asset.delete / knowledge.sync / knowledge.retry / knowledge.cancel / knowledge.activateVersion`（`knowledge/api.ts:210-258`、`knowledge/components/api-actions.tsx`）。
导入资料：`ImportDrawer` → 上传文件后调 `asset.register`（前端不解析 PDF/图片内容，服务端登记）。

### 2.8 报告归档 `/archive`
| 动作 | 结果 |
|---|---|
| 「运行交付文件校验」 | `POST /api/archives/check`（服务端流式读字节重算 SHA-256，`Archive.tsx:98`、`http.mjs:1027-1070`） |
| 「下载校验报告」 | 客户端拼 HTML 并下载（`Archive.tsx:120`、`lib.ts:611`） |
| 「补传 / 重选副本」 | `POST /api/files`（真字节）+ `POST /api/archives/repair`（`Archive.tsx:139-142`） |

### 2.9 大屏 `/present` / 控制台 `/console`
- `/present` 无交互，只按 `focus` 渲染（`focus.ts`，读共享快照里的 projection）。
- 顶栏「投到展示窗口」：`POST /api/projection`（非持有人 409 → 二次确认后带 `hold:true` 接管），成功 `window.open('#/present')`（`Shell.tsx:390-424`、`focus.ts`）。
- `/console`：「新建演示会话」`POST /api/console/sessions`；「捕获当前状态」`POST /api/console/snapshots`；「恢复」`POST /api/console/snapshots/restore`；「删除」`.../delete`；「导出诊断包」`GET /api/console/diagnostics`（`Console.tsx:60-101`）。

---

## 3. 数据来源三分法

图例：**(A)** 后端真实接口 · **(B)** 前端 seed/夹具/常量 · **(C)** 混合（注明哪部分是 seed）。

| 页面 | 判定 | 说明（含文件） |
|---|---|---|
| **总览 `/`** | **C（以 B 为主）** | 地图与点位=seed（`seed/sites.ts`：全国 28 处、上海 12 处 + `assets/map/*.json` 真实 GeoJSON）；巡检概览/工单看板=seed（`seed/scenario.ts` 的 `WORK_ORDER/HISTORIC_ORDERS/CURRENT_RISKS`）；**平台数据=真实后端主机采样**（`usePlatformResources.ts` → `/api/platform/resources`）；设备状态=真实通道状态（`useMumai().channels`）+ `DEVICES` 名称（seed） |
| **工单 `/orders`** | **C** | 新流程列表/详情/指派/环境/下发/**全部真实**（`store/workOrders.ts` → `/api/work-orders*`）；旧「演示回放」工单、构件清单、任务附件、操作日志、人员分工、`CONFIG_DIFF` 全部 seed（`seed/scenario.ts`）；旧单环境历史来自共享会话实体（真实） |
| **建图巡航 `/mapping`** | **A** | 状态 WS + `/api/cart/status`、`/api/cart/read/*`、`/api/cart/action`、`/api/cart/map.png`、`/api/cart/stream/*`（`pages/cart/api.ts`）。页面无 seed 兜底 |
| **数字孪生 `/twin`** | **C** | 场景版本=服务端 scene 实体（真实，`store/shared.ts:212`）；3DGS 模型=真实文件（`/api/files/:id/model/:name`）；**热点、回波、融合规则、历史任务、波形全部 seed**（`Twin.tsx:205-232`）；场景检查/发布=真实命令 |
| **硬件详情 `/hardware`** | **C（真机优先，离线静默回落 seed）** | 服务端接口齐备（`/api/devices/*`、`/api/sensors/*`）；**设备离线时通道与批次回落 `CHANNELS`/`SCAN_BATCHES`，版本格回落 `VERSION_ITEMS`**（`Hardware.tsx:202-210,408-413`）；异常排查与采集作业的批次/波形/启动检查=seed（`seed/deviceLogs.ts`、`seed/scenario.ts`） |
| **固件及模型 `/firmware`** | **B** | 版本矩阵 `seed/versions.ts`；数据集/清洗 `seed/scenario.ts` + `cleanLogic.ts`；训练 `TrainingRun.tsx` 零 API，全部 seed（`EXPERIMENT/FAILED_EXPERIMENT/TRAIN_NODE/IMPORTABLE_PACKAGES`）；更新交付「待提交产物」=seed，**「已发布产物」=服务端 artifact 实体**（`DeliveryCenter.tsx:234`）；融合分析=seed（`FUSION_RECORD`） |
| **知识库 `/knowledge`** | **A** | 全部走 `/api/knowledge/*` + 命令总线；实测后端有 167,110 条资产（1,500 条可展开）、18,000 分块/向量。仅「数据说明」抽屉里的夹具报告措辞来自 `seed/scenario.ts:2089` 的文档内容 |
| **报告归档 `/archive`** | **C** | 清单与校验=服务端 `archiveItem` 实体 + `POST /api/archives/check`（实测 24 项 / 通过 21 / 缺失 1 / 不一致 2）；更新包区块=seed `UPDATE_PACKAGE`；报告文件名里的工单号=seed `WORK_ORDER.id` |
| **大屏 `/present`** | **C** | 展示焦点=服务端 projection（真实）；内容数据绝大多数 seed，仅「更新交付」视图读服务端 artifact 实体（`Present.tsx:277-345`） |
| **控制台 `/console`** | **A** | 全部 `/api/console/*` |
| **小木（全局浮层）** | **B（零业务后端）** | 意图 29 条、工具 20 个、剧本 22 轮全部前端本地；唯一网络：`/voice/manifest.json` 静态音频清单 + `/voice-wake` WS（唤醒链路，服务不在本仓库） |

---

## 4. 能力清单（逐项明确回答）

| 剧本里的功能 | 有没有 | 在哪 | 名字 / 证据 |
|---|---|---|---|
| **工单创建** | ✅ 有（两条路） | `/orders`；隐藏快捷键 | ① 真后端：`Ctrl+Q+L` → `POST /api/work-orders/trigger`（`useWorkOrderShortcut.ts:104`，**页面上无任何提示与入口**）。② 前端本地：「生成工单」按钮 → 弹窗表单 → 只写浏览器内存（`Orders.tsx:428`、`WorkOrderCreate.tsx:26`、`context.tsx:241-265`） |
| **工单识别 / 读取附件** | ❌ **未找到** | — | 全仓无 OCR/附件解析/识别相关实现（grep `工单识别|OCR|解析附件|读取附件` 仅命中无关词）。工单详情只有「原始委托附件」**只读列表**（名称/类型/大小，`WorkOrderDetail.tsx:209-226`），不能打开、不能解析；也没有附件上传入口 |
| **人员指派 / 分工** | ✅ 有（真后端） | `/orders` → 新工单详情 | 「负责人及参与人员」面板（`orders/AssignmentPanel.tsx:57-160`）+ 指派弹窗 `AssignModal:169-`，选负责人 + 参与人 + 逐人职责 → `PUT /api/work-orders/{id}/assignment`。旧演示单的人员分工是只读 seed（`Orders.tsx:845-854`） |
| **设备（机器人小车 / 手持扫描仪）管理页** | ⚠️ 半个 | `/hardware`（扫描仪）、`/mapping`（小车） | **没有设备列表/管理页**。设备台账接口 `GET /api/devices` 存在（`http.mjs:332`），前端 `api.deviceLedger()` 定义了但**从未被调用**（`api/client.ts:740`）。扫描仪设备号写死 `HANDHELD_DEVICE_ID="handheld-02"`（`device/types.ts:31`），可 `?device=` 覆盖；小车只在建图巡航页出现 |
| **设备详情** | ✅ 有 | `/hardware`（三页签） | 设备状态 / 设备读数 / 通道状态 / 采集接收 / 终端遥测 / 设备预览（`Hardware.tsx:851-874`） |
| **设备状态遥测** | ✅ 有（真机优先） | 同上 + 顶栏 | 终端 2s 上报 `POST /api/devices/{id}/hardware`，页面 `useDeviceLink` 轮询读（`device/useDeviceLink.ts`）；顶栏四格状态（`Shell.tsx:251-371`）；设备离线时回落 seed（见第 6 节） |
| **环境记录（温湿度风速）录入与展示** | ✅ 有（两套） | `/orders` | 新单：四项（温度/相对湿度/风速/大气压，`orders/EnvironmentPanel.tsx:39-44`），草稿→校验版本→下发，真后端；旧单：三项 + 仪表/位置/时间（`Orders.tsx:919-987`），走共享会话命令。**没有环境记录列表页/历史曲线图**，历史只列版本行（`Orders.tsx:791-828`） |
| **地图管理：SLAM 建图** | ✅ 有（真后端，需小车在线） | `/mapping` | 「开始建图/结束建图/重置地图/保存地图」（`Mapping.tsx:548-596`）→ `POST /api/cart/action` `mapping/*` |
| **地图版本台账** | ⚠️ 只有 seed | 无独立页面 | `MAP_VERSIONS`（`seed/scenario.ts:675`）只在顶栏 tooltip 与小木里用；`store/shared.ts:208` 的 `mapVersions` 选择器**无人调用** |
| **地图列表** | ✅ 有（真数据） | `/mapping` → 「已保存地图与路线」 | 读 `GET /api/cart/read/maps` 与 `/routes`（`Mapping.tsx:210`），支持预览、载入、下载 `.pgm/.yaml`（`Mapping.tsx:1108-1120`） |
| **视频流 / 直播** | ✅ 有 | `/mapping`、采集作业、硬件监看 | RViz MJPEG `/api/cart/stream/rviz`、摄像头 `/api/cart/stream/camera`（`cart/api.ts:334`）；采集端屏幕 `/api/capture/screen/stream`（`CaptureScreen.tsx:37`）；设备单帧预览 `/api/devices/{id}/preview/latest`（`useDeviceLink.ts:145`） |
| **录像回放** | ❌ **未找到** | — | 无录像文件、无回放器、无时间轴。仅有实时 MJPEG 流与单帧预览 |
| **三维场景 / 3DGS 预览** | ✅ 有（真渲染） | `/twin` | Spark + SplatMesh（`SplatStage.tsx:22`、`package.json` 的 `@sparkjsdev/spark ^2.1.0`）；仓库内有真实产物 `public/model/sog/gs.sog`（31.7 MB，约 238 万点）；带进度、失败重试、LoD |
| **构件标注（3DGS 里的锚点）** | ❌ **实际上做不到** | — | `SplatStage` 的 props 只有 `url/transform/camera/onProgress/onLoaded/onError/fitNonce/active`（`SplatStage.tsx:422-444`），**没有锚点叠加能力**；`Twin.tsx:273` 传 `camera={null}`，不会聚焦构件。上传时 `componentAnchors` 恒传空数组（`Twin.tsx:656`）；实测线上 scene 实体：绑定工单的那条 `anchors=0`，seed 那三条虽有 4 个锚点但 `position` 全为 `null` 且无文件。**连带后果**：`scene.check` 要求锚点>0 且书签>0（`workflow.mjs:358-365`），所以「上传模型→运行检查→发布场景」在当前 UI 下**永远无法通过检查** |
| **数据接收 / 上传** | ⚠️ 部分 | 多处 | 上传：`POST /api/files`（一次性投递，`Twin.tsx:644`、`DeliveryCenter.tsx`、`Archive.tsx:140`、Knowledge 导入）。**分片上传三步**（`POST /api/files/uploads` → `PUT .../chunks` → `POST .../complete`，`uploads.mjs:972-1004`）**前端从未调用**（grep 无命中），只有终端/`tools/test-uploads.mjs` 用 |
| **批次管理** | ⚠️ 部分 | `/hardware` 采集接收 | 批次接收进度实时展示（三路实收/应收，`Hardware.tsx:555-677`）+「补传」；`POST/GET /api/batches`（批次清单，`uploads.mjs:1005-1019`）**前端未调用**；没有批次列表页，批次只是页面上下文（`?batch=`，数据来自 `SCAN_BATCHES` seed） |
| **数据集管理** | ⚠️ 有界面，纯前端 | `/firmware?tab=dataset` | 样本清单表、本轮版本表、分组检查（`adaptTabs.tsx:129-200+`）。注意：`/knowledge` 的"数据资产"是**资料/知识资产**，不是训练数据集 |
| **数据清洗** | ⚠️ 有流程，纯前端 | 同上 | `DatasetCleanFlow.tsx:40-460`：选原始数据集→配置算法与阈值→预检查→执行清洗→输出疑似异常→人工核验→生成新版本；规则在 `cleanLogic.ts`（`DEFAULT_THRESHOLDS/runClean`），样本来自 seed `SAMPLES`；"生成版本"只写组件 state（`adaptTabs.tsx:67,127`），刷新即丢 |
| **标注审核** | ❌ **未找到** | — | 只有只读的「图像标注」表（标注框/图像/标签/置信度/测区，`adaptTabs.tsx:391-400`，数据 `FUSION_RECORD.annotations`），**没有审核队列、通过/驳回按钮**。清洗流程里的"人工核验"步骤（`DatasetCleanFlow.tsx`）是最接近的一处 |
| **数据集划分 train/val/test** | ✅ 有（前端） | `/firmware?tab=dataset` | 分组检查三集合 + 「编入所选集合」「整组调整后重跑」，`lib.checkGrouping` 现算交集/冲突（`adaptTabs.tsx:59-118`） |
| **模型训练 / 微调任务** | ⚠️ 有界面，无后端 | `/firmware?tab=training` | 「配置并提交」→ 训练配置弹窗（`TrainingRun.tsx:221,262-292`）；提交后按 seed 脚本逐行回放日志（`LINE_INTERVAL_MS=260` `:77`）。**全文件零 API 调用** |
| **损失曲线** | ✅ 有图（seed） | 同上 | 训练/验证/基线三条曲线 + 过拟合发散点现算（`TrainingRun.tsx:726-783`） |
| **模型评估对比** | ✅ 有界面（seed） | 同上 | 「独立测试集对比」：新/旧分、召回、漏检误报、按材种回归、逐样本预测对比、验收规则（`TrainingRun.tsx:1179-1340`），`lib.runEvaluation` |
| **模型量化** | ❌ 只有文字 | 无 | 量化只作为**记录文字**存在（`seed/versions.ts:166` INT8 量化、`seed/scenario.ts:1706-1718`）；`DeliveryCenter.tsx:7-8` 注释说明原「量化与兼容性」面板已被删除。没有量化任务/参数/产物生成 |
| **更新包封装** | ⚠️ 有界面，提交不落库 | `/firmware?tab=delivery` | 「待提交产物 / 提交前校验 / 已发布产物」三块（`DeliveryCenter.tsx:441-660`）；「提交」**只改本地 state**（`:249-265`），不发请求 |
| **固件/模型下发与回执** | ✅ 有（工单域） | `/orders` → 「扫描仪下发」 | 下发 `POST /api/work-orders/{id}/dispatches`；回执由设备事件推进（`http.mjs:315-330` 把 accepted 事件喂给工单域）；历史下发记录表含创建/过期/接收/应用时间（`orders/DispatchPanel.tsx:226-244`）；设备侧取包 `GET /api/devices/{id}/work-order-bundles/pending`（`http.mjs:520`）。更新交付里的「回验」也是真的（`DeliveryCenter.tsx:307-330`） |
| **数字孪生关联（构件↔场景↔检测结果）** | ⚠️ 只有字符串匹配 | `/twin` | 工单→场景：服务端 `orderId` 绑定（`workflow.mjs:313-316`）；构件→热点/风险/波形：前端用 `componentId` 在 seed 里 find（`Twin.tsx:205-232`）。**没有坐标级关联**（锚点 position 全 null，`Twin.tsx:656` 上传恒空） |
| **报告生成** | ⚠️ 只有一种 | `/archive` | 仅「归档完整性报告」HTML，客户端拼装下载（`Archive.tsx:40-42,120,152-200`，`lib.ts:611`）。**没有检测报告 / 巡检报告模板与生成** |
| **工单流转状态** | ✅ 有（新旧两套） | `/orders` | 新单：服务端状态机 `待准备/待作业/作业中/待验收/已归档/已暂停` + 动作 `开始作业/提交验收/验收通过并归档/归档/暂停/恢复`（`server/services/work-orders.mjs:975-980`，按钮由 `capabilities` 控制 `WorkOrderDetail.tsx:35-42,128-137`）。旧单：前端本地 `草稿/待复核/待处理/处理中/待验收/已完成/已关闭`（`design.ts:160-168`），只有「暂停/归档」两个按钮 |
| **复巡计划** | ❌ **未找到功能** | — | 只有 seed `REVISIT_PLAN`（`seed/scenario.ts:1811-1826`，`dispatched:false`），被小木意图"安排下次复巡"读取（`agent/facts.ts:303-310`），以及旧工单 KV 显示 `revisitPlanId`（`Orders.tsx:569`）。没有页面、没有创建、没有下发 |
| **AI 助手「小木」** | ✅ 有（全前端） | 全局浮层 | 见下节 |

### 4.1 小木（Xiaomu）能力详情

- **能力本质**：**本地规则 + 意图匹配 + 本地模板渲染 + 逐字剧本**。**不调用任何 LLM/大模型 API**（代码显式声明：`agent/matcher.ts:9`、`agent/planner.ts:4-5`、`agent/intents.ts:472`、`SmallWoodPanel.tsx:457`）。
- **网络出口只有两个**：`fetch("/voice/manifest.json")` 静态音频清单（`agent/voicePack.ts:24,39`）与 `WebSocket /voice-wake`（`agent/wakeChannel.ts:557,560`，代理到 `127.0.0.1:8780` 的语音边车，`vite.config.ts:105-108`；**该服务不在本仓库**）。回复生成链路 0 个 HTTP。
- **意图 29 条**（`agent/intents.ts:132-786` 共 28 条 + `:808` 多步编排 1 条），示例语料 151 条。代表性意图与动作：

| 意图 | 中文说法示例 | 动作 |
|---|---|---|
| `open_page` | "打开地图/打开知识库" | 前端路由跳转 |
| `view_current_order` | "查看当前工单" | `/orders?order=` |
| `view_four_pillars` | "查看四柱状态" | `/twin?component=` |
| `view_echo` / `view_batch` | "查看回波 / 查看复扫批次" | `/hardware?tab=capture&batch=` |
| `draft_workorder` | "生成复核工单" | 打开工单草稿 |
| `create_revisit` | "安排下次复巡" | 打开数据集/复巡面板（本地） |
| `clean_dataset` / `prepare_split` | "启动清洗并列出待审核 / 生成数据集划分" | 跳 `/firmware?tab=dataset` |
| `compare_models` | "对比新旧模型" | 跳 `/firmware?tab=training` |
| `run_fusion` | "分析本批次并融合结果" | 跳 `/firmware?tab=fusion` |
| `robot_move` / `start_patrol` / `robot_stop` / `cancel_patrol` / `robot_return_home` | "让小车去一号木柱 / 开始巡检 / 紧急停止" | **只改本地演示快照**（见下） |
| `start_mapping` / `load_map` | "开始建图 / 装载地图版本" | **只改本地演示快照** |
| `device_status` | "小车现在电量多少" | 读本地演示快照 |
| `introduce_platform` / `history_summary` / `site_weather` 等 | 问答类 | 用 seed 事实填模板回复 |

- **工具 20 个**（`agent/tools.ts:149-581`）：`navigate_page / open_panel / focus_component / open_order / open_scene / switch_map_mode / set_map_region / robot_move / robot_circle_target / robot_stop / cancel_patrol / robot_return_home / start_patrol / get_robot_status / get_map_list / load_map / start_mapping / start_scan / stop_scan / get_camera_status`。**没有一个是访问后端的**：导航类改前端路由，机器人/地图/扫描类只 `ctx.live.patch(...)` 写一个前端演示快照（`agent/store.ts:22-34`、`agent/executor.ts:724`）。
  - 例：`robot_stop` 注释直写"只改任务状态与提示，**不驱动真实设备**"（`tools.ts:387`）；`load_map` 注释"演示为切换展示版本，**不触发真实 SLAM 载入**"（`tools.ts:485`）；`start_mapping` 注释"写实时快照的 mapping 标志，地图版本来自种子"（`tools.ts:507`）。
  - 风险分级 0–3，`requireConfirmation` 7 个（高风险工具执行前弹确认）。
- **固定剧本 22 轮**（`agent/script.ts:82-586`，触发说法 89 条，其中 6 轮带语音编号），按用户说法**逐字匹配**后优先走剧本（`agent/executor.ts:652-658`、`agent/scriptMatch.ts:242-266`）。
- **固定话术**：未命中 `FALLBACK_TEXT`"我没有理解你的指令，可以换一种说法。"（`intents.ts:124`）；未听清"没有听清，请再说一次。例如可以说「查近三个月天气」。"（`degrade.ts:57-63`，且**不调用任何工具**）；降级话术 6 条（`degrade.ts:57-76`）。
- **唤醒**：唤醒词「小木小木」；**判定不在前端**——前端只采音频并推送，判定留给服务端（`wakeChannel.ts:17-21,370`，P0 阶段"尚未做唤醒判定"）。快捷键 `Alt+W/E/R/M`（`XiaomuDock.tsx:377-399`）。
- **TTS 是真的**：60 条预生成 mp3（`public/voice/*.mp3`，实测 60 个）→ 未命中回落 `speechSynthesis` → 再不可用则只显示字幕（`agent/tts.ts:1-22`、`agent/voicePack.ts`）。
- **ASR 两条真 + 一条模拟**：浏览器 `webkitSpeechRecognition`（`agent/asr.ts:138-142,429-494`）、唤醒链路服务端转写（`wakeChannel.ts:623-644`）、**脚本化模拟**（点示例问句时逐字吐 partial，`asr.ts:508-539`）。
- **文字入口 vs 语音入口**：右下角浮层（`agent/XiaomuDock.tsx`）与顶栏「小木」面板（`SmallWoodPanel.tsx`，文字面板**不播报** `:245`）。

---

## 5. 平台侧后端真实接口全表

来源：`server/api/http.mjs`（77 条）+ `server/services/uploads.mjs`（5 条，由 `registerUploadRoutes` 注册，`http.mjs:564`）= **82 条**。
另有 3 个 WebSocket 入口（`server/index.mjs:94-98`）：`/ws`（浏览器事件通道）、`/ws/devices/{deviceId}`（设备通道）、以及 vite 代理的 `/voice-wake`、`/voice-asr`（**语音边车，不在此仓库**）。

### 5.1 采集端屏幕（2）
| 方法 | 路径 | 作用 | 行号 |
|---|---|---|---|
| GET | `/api/capture/screen/status` | 采集端推流状态 | `http.mjs:171` |
| GET | `/api/capture/screen/stream` | 代理采集端屏幕 MJPEG | `:172` |

### 5.2 小车 cart（8 + 1 WS）
| 方法 | 路径 | 作用 | 行号 |
|---|---|---|---|
| GET | `/api/cart/status` | 小车状态快照（`configured/canControl/link/state`） | `:204` |
| GET | `/api/cart/info` | 刷新并返回小车信息 | `:205` |
| GET | `/api/cart/read/:channel` | 只读通道（`health/session/config/maps/routes`…） | `:207` |
| GET | `/api/cart/map.png` | 当前栅格地图 PNG（免登录） | `:216` |
| GET | `/api/cart/maps/:mapId/:file` | 已保存地图文件转发（png/yaml/pgm/json，免登录） | `:232` |
| GET | `/api/cart/stream/:channel` | RViz / 摄像头 MJPEG 代理（免登录） | `:261` |
| POST | `/api/cart/action` | 控制动作转发（白名单 14 个动作，`services/cart.mjs:81-95`） | `:276` |
| WS | `/ws`（`kind:"cart"` 帧） | 2Hz 状态推送 | `services/cart.mjs`/`hub.mjs` |

### 5.3 设备网关（11）
| 方法 | 路径 | 作用 | 行号 |
|---|---|---|---|
| POST | `/api/devices/register` | 终端注册（设备令牌，免登录） | `:313` |
| POST | `/api/device-events/batch` | 设备事件批量上行 + 回执喂给工单域 | `:315` |
| GET | `/api/devices` | 设备台账（登录态或设备令牌） | `:332` |
| POST | `/api/devices/:deviceId/hardware` | 终端上报硬件/读数/批次（免登录） | `:340` |
| GET | `/api/devices/:deviceId/hardware` | 读最新硬件视图 | `:343` |
| GET | `/api/devices/:deviceId/history` | 硬件历史采样 | `:349` |
| GET | `/api/devices/:deviceId/events` | 设备事件列表 | `:354` |
| POST | `/api/devices/:deviceId/commands` | 下发设备命令（需 `scan:capture`/`console:admin`） | `:359` |
| POST | `/api/configs/:configVersion/ack` | 现场调参回执登记（免登录） | `:371` |
| POST | `/api/devices/:deviceId/preview` | 低帧率预览图二进制上行（免登录） | `:375` |
| GET | `/api/devices/:deviceId/preview/latest` | 最新预览帧 JPEG（无帧回 404） | `:381` |

### 5.4 工单域（11）
| 方法 | 路径 | 作用 | 行号 |
|---|---|---|---|
| POST | `/api/work-orders/trigger` | 快捷键触发建单（按 eventId 幂等） | `:421` |
| GET | `/api/work-orders` | 工单列表（filter/q）+ 账号表 + 下发目标 | `:437` |
| GET | `/api/work-orders/:orderId` | 工单详情（含 `capabilities`） | `:446` |
| PUT | `/api/work-orders/:orderId/assignment` | 指派负责人与成员职责（乐观锁 revision） | `:451` |
| POST | `/api/work-orders/:orderId/status` | 状态机动作 | `:463` |
| PUT | `/api/work-orders/:orderId/environment-draft` | 保存环境草稿 | `:474` |
| POST | `/api/work-orders/:orderId/environment/validate` | 校验并生成不可变配置版本 | `:485` |
| POST | `/api/work-orders/:orderId/dispatches` | 下发扫描仪（幂等 key） | `:495` |
| GET | `/api/work-orders/:orderId/dispatches` | 下发记录 | `:508` |
| DELETE | `/api/work-orders/:orderId` | 删除工单（级联，不可恢复） | `:514` |
| GET | `/api/devices/:deviceId/work-order-bundles/pending` | 设备拉取待执行包 | `:520` |
| GET | `/api/devices/:deviceId/environment` | 终端取本单已校验环境读数 | `:529` |
| GET | `/api/work-order-bundles/:bundleId` | 包下载（按摘要固化的原始字节） | `:538` |

### 5.5 传感器 / BLE（9）
`GET /api/sensors/bridge` `:565`、`POST /api/sensors/gyro-calibrate` `:566`、`GET /api/sensors/calibration/:id` `:571`、`POST /api/sensors/calibration/:id` `:572`、`POST /api/sensors/scan` `:576`、`POST /api/sensors/connect` `:577`、`POST /api/sensors/disconnect` `:582`、`POST /api/sensors/frames` `:584`（**前端未调用**）、`GET /api/sensors/latest` `:591`、`GET /api/sensors/history` `:595`。

### 5.6 认证 / 会话 / 命令总线（6）
`POST /api/auth/login` `:612`、`GET /api/auth/me` `:626`、`GET /api/sessions` `:634`、`POST /api/sessions` `:636`、`GET /api/sessions/:id/snapshot` `:642`、`GET /api/events` `:647`（**前端未调用**，WebSocket 替代）、`POST /api/commands` `:655`（命令总线，含幂等 commandId 与 revision 冲突）。

### 5.7 知识中心（9）
`GET /api/knowledge/overview` `:718`、`GET /api/knowledge/assets` `:728`、`GET /api/knowledge/assets/:id` `:746`、`GET /api/knowledge/graph` `:752`、`GET /api/knowledge/indexes` `:763`、`GET /api/knowledge/jobs` `:780`、`GET /api/knowledge/jobs/:id` `:784`、`POST /api/knowledge/search` `:791`、`GET /api/knowledge/fixture` `:809`。

### 5.8 文件 / 环境校验 / 上传批次（9）
`POST /api/files` `:832`（raw body 直传）、`GET /api/files/:id` `:870`、`GET /api/files/:id/verify` `:885`、`GET /api/files/:id/download` `:899`、`GET /api/files/:id/model/:name` `:959`、`POST /api/environments/validate` `:816`；分片上传与批次（`uploads.mjs`）：`POST /api/files/uploads` `:972`、`PUT /api/files/uploads/:uploadId/chunks` `:987`、`POST /api/files/uploads/:uploadId/complete` `:996`、`POST /api/batches` `:1005`、`GET /api/batches` `:1014`。

### 5.9 投屏 / 归档 / 控制台 / 平台资源 / 健康（11）
`POST /api/projection` `:983`（非持有人 409）、`POST /api/archives/check` `:1027`、`POST /api/archives/repair` `:1079`、`GET /api/console/overview` `:1132`、`POST /api/console/sessions` `:1154`、`POST /api/console/snapshots` `:1174`、`POST /api/console/snapshots/restore` `:1201`、`POST /api/console/snapshots/delete` `:1219`、`GET /api/console/diagnostics` `:1229`、`GET /api/platform/resources` `:1245`（免登录）、`GET /api/platform/resources/history` `:1251`（免登录）、`GET /api/health` `:1260`（免登录）。

### 5.10 命令总线动作（`POST /api/commands` 的 action 白名单）
`environment.publish / environment.receive / mission.create|ack|pause|resume|cancel|complete / map.save / scene.submit / scene.check / scene.publish / artifact.build / artifact.publish / artifact.receipt / projection.set / projection.hold / asset.register|revise|updateMetadata|setInclusion|delete / knowledge.sync|retry|cancel|activateVersion|configure`（`server/services/workflow.mjs:197-520`、`server/services/permissions.mjs:14-70`、`server/services/knowledge-jobs.mjs`）。

---

## 6. 可疑 / 未完成 / 写死 / 占位

### 6.1 伪成功与静默回落（最该在剧本里避开的）
1. **「补传」离线时伪成功**：`Hardware.tsx:584-588` —— `!live` 时直接 `toast("已向扫描枪请求重传未接收分片","info")` 然后 return，**不发任何请求**。
2. **硬件页静默回落到种子**：通道 `report?.channels?.length ? report.channels : CHANNELS`（`Hardware.tsx:202-205`）、批次回落 `SCAN_BATCHES`（`:207-210`）、版本四格回落 `VERSION_ITEMS`（`:408-413`）—— 且这三处**不挂来源标记**（`SourceTag` 只在 live 时渲染 `:514`）。
3. **注释与实现直接矛盾**：`Hardware.tsx:14`「真机优先，种子兜底」、`:26-27`「不静默回退成种子」、`:174-176`「没有上报时不退回演示数据」↔ 上面第 2 条。
4. **未上报时显示「已连接 · 只读监视」绿点**：`Hardware.tsx:367-372`（此时设备实际是离线/从未上报）。

### 6.2 写死 / 硬编码
5. 扫描枪电量写死 `FIXED_BATTERY_PCT = 83`，但**如实标注**"写死 / 演示固定值"（`sensors/SensorWorkspace.tsx:32,172`）。
6. 相对湿度兜底常量 `FALLBACK_HUMIDITY = 58` + localStorage 缓存三级（`sensors/weather.ts`，标签区分 live/cache/fallback）。
7. 演示口令 `"123456"` 硬编码 **3 份**：`api/client.ts:1165`、`store/shared.ts:64`、`auth.ts:333`；登录页本身**不过服务端**（`auth.ts:508`、`pages/Login.tsx:269-282`）。
8. 界面文案里的会话号 `session-A` 与真实会话 `demo-01` 不一致：`context.tsx:314`（文案）vs `store/shared.ts:62`（真实请求）vs `server/services/session.mjs:17`（服务端默认）。
9. `scene.submit` 上传时 `componentAnchors: []`、`bookmarkIds: []` 恒为空（`Twin.tsx:656`），导致场景检查永远不通过（`workflow.mjs:358-365`）。
10. 训练/清洗/融合/交付等页面大量"演示用稳定伪随机"与种子数值（`lib.ts:785` `seededRandom`）。

### 6.3 定义了但没有任何调用方（死代码 / 未接线）
11. 前端函数：`api.health`（`client.ts:639`）、`api.fileMeta`（`:542`）、`api.verifyFile`（`:548`）、`api.deviceLedger`（`:740`，即**设备台账页没做**）、`api.createSession`（`:479`）、`knowledgeApi.indexes`（`knowledge/api.ts:133`）、`knowledgeApi.fixture`（`:176`）。
12. 常量/导出：`SPLAT_URL`（`pages/splat.ts:35`，无人 import）、`TODO_ITEMS`（`seed/scenario.ts:1919`）、`store/shared.ts:208` 的 `mapVersions` 选择器。
13. 整个 `pages/MumaiDashboard/index.tsx`（335 行旧版单页大屏）+ `WorkOrderModal.tsx`（19 行）+ `ModuleDrawer.tsx`（13 行）+ `data.ts` 里的 `workOrders/moduleCopy`：**已不参与路由**，仅作为旧布局参考保留（`index.tsx:1-7` 自述）。

### 6.4 显示 bug / 逻辑瑕疵
14. 「作业记录与成果」面板**无条件渲染空态**：先渲染 `StateBlock "本单还没有作业记录"`，再渲染日志列表（`WorkOrderDetail.tsx:289-301`）—— 有日志时也会同时看到"还没有记录"。
15. `SiteDetailCard.tsx:226` 跳 `/orders?create=1`，但 Orders 页只读 `params.get("order")`（`Orders.tsx:160`）——**该参数无人处理**，点「建单」只是进了工单页，不会打开建单弹窗。
16. `TrainingRun.tsx` 提交训练任务后**没有任何后端记录**，刷新即回到初始状态。
17. `DeliveryCenter`「提交」只改本地 state（`:249-265`），切页/刷新后产物回到"待提交"（"已发布"列表读服务端 `:234`，所以会出现两套口径并存）。
18. `Knowledge` 页自建了第二套请求器（`knowledge/api.ts:35-77`），**没有 401 自动补登录**（与 `api/client.ts:336-350` 不一致）。

### 6.5 小木侧的未完成项（均由子调研逐条核对）
19. `agent/script.ts:24` 承诺的 `demoNote` 字段**未落地**（类型 `:28-65` 无此字段）。
20. `agent/intents.ts:81-85` 的 `audio` 字段是**死字段**，全库无任何意图赋值；`VoiceConsole.tsx:713` 传入的 `lastSpokenRef.current.audio` 永为 `undefined`。
21. `agent/degrade.ts:32-36` 有一处**已失效的 TODO**（指向的行号早已变成别的代码）。
22. `agent/tts.ts:5` 的注释"本仓库没有任何音频素材（public/ 下没有 audio 目录）"**已过时**（现有 60 条 mp3）。
23. 文档与代码计数不一致：`agent/script.ts:10,79` 写"20 轮"实际 22 轮；`agent/intents.ts:4-8` 写"14+7 条"实际 29 条。
24. 唤醒判定尚未实现（`agent/wakeChannel.ts:17-21,370` 自述 P0 只采音频），且 `/voice-wake` 语音边车**不在本仓库**（`vite.config.ts:105-108` 指向 `127.0.0.1:8780`）。

### 6.6 其余值得注意的工程事实
25. 同一页面最多开 **3 条独立 WebSocket**（共享事件流 `store/shared.ts:101`、小车 `pages/cart/api.ts:409`、传感器 `sensors/useSensor.ts:80`），各自退避、互不共享。
26. `/ws` 连接**不带令牌**，服务端也不校验（`api/client.ts:1090-1093`、`server/services/hub.mjs`）。
27. 下载实现是"整包读进内存"（`api/client.ts:583-585` 自述，大文件应改成一次性签名地址）。
28. 知识中心页面文案与实际不符：`seed/scenario.ts:2089` 的演示文档写"本页不连接后端、不调用嵌入模型…均为前端演示流程"，而当前 `/knowledge` 已全量走 `/api/knowledge/*`（属遗留文案）。
29. `docs/` 与 `seed/` 中多处日期、版本号、批次号为**演示剧本锚点**（如 `seed/deviceLogs.ts:42` 自述"手工写死的锚点，精确落在与剧本一致的时间点"）。

---

## 7. 运行时实测（2026-09-16，`http://127.0.0.1:8000`）

| 项 | 实测值 | 含义 |
|---|---|---|
| `/api/health` | `ok:true`、`sessions:2`、`clients:1`、`devices:{devices:2, online:0, tokens:1}` | 服务在跑；**2 台设备已登记但 0 台在线** |
| `/api/cart/status` | `configured:true`、`canControl:true`、`link:"offline"`、`lastError:"connect ETIMEDOUT 192.168.31.221:8765"`、`state` 是上一次成功连接的缓存 | 小车令牌已配（`server/data/cart.json`），但**此刻连不上小车** → 建图/巡航页所有动作会置灰并显示「状态不新鲜」 |
| `GET /api/devices` | `mumai-car-01`（robot，offline，lastSeen 2026-09-14T16:01Z）、`handheld-02`（offline，2026-09-14T15:06Z） | 扫描仪与小车都离线 → 硬件页会走 seed 兜底 |
| `GET /api/work-orders` | 2 张真实工单：`WO-20260915-0001`（待准备）、`WO-20260914-0001`（待作业，`dispatchState:"executed"`） | 快捷键建单链路真的写库了，且下发已到 executed |
| `GET /api/knowledge/overview` | total 167,110 / materialized 1,500 / chunks 18,000 / vectors 18,000 / servingVersion `KB-021` | 知识库是真的（含"规模样本"，只有 1,500 条能展开明细） |
| `POST /api/archives/check` | total 24 / passed 21 / missing 1 / mismatch 2 | **服务端真的读字节重算 SHA-256**（1 缺失 2 不一致是故意留的） |
| `GET /api/platform/resources` | `hostId:"host-darwin"`、`serverCount:1`、`storageTotalTB:24.35`、`memoryTotalGiB:672`、`quality:"unavailable"` | 平台数据取自**本机真实采样**（这台 Mac），GPU 等项不可用时如实标 unavailable |
| 会话 `demo-01` 快照 | entities：`archiveItem:24, artifact:1, environment:4, mapVersion:1, scene:4` | 平台会话里**没有** dataset / experiment / deviceLog / training 等实体 —— 再次印证这些是纯前端 |
| 绑定工单的场景 `SCN-MU0SDUE3` | `orderId=SH-2026-0901`、`assetFileId=file-a291…`（真实 31.7MB `.sog`）、`anchors=0`、`state=待检查` | 3DGS 能看，但**没有任何构件锚点**，场景检查必然不通过 |
| seed 场景 `scene-SH-0901` / `scene-May` / `-raw` | `orderId=null`、`assetFileId=null`、`anchors=4` 但 `position` **全为 null** | 有锚点元数据但无坐标、无文件，且不绑定工单 → 孪生页不显示 |
