# 交接说明 · 小木语音交互 / 形象 / 验收（2026-09-14）

> 用途：**新开对话时先读这一份**，即可无缝接上进度。
> 本文档记录到 2026-09-14 12:5x 为止的状态。仓库：`D:\平台\Wood-Grain-Quality-Inspection-Smart-Platform-RAO\Wood-Grain-Quality-Inspection-Smart-Platform-RAO`（git 分支 **SHI**）。

---

## 0. 一分钟上手（环境事实，必须先知道）

| 项 | 值 |
|---|---|
| 分支 | **SHI**（`origin/SHI` 已推到 `9b63b39`；`main` / `RAO` 一律不碰） |
| **GitHub 直连不通** | fetch/push 必须一次性走本机代理：`git -c http.proxy=http://127.0.0.1:7890 push origin SHI`（**不要写进持久 git 配置**） |
| 端口 | 5173 vite 前端 / 8000 共享服务（`node server/index.mjs`）/ 8770 Python ASR+KWS / 8780 Node 语音桥（后两者只监听 127.0.0.1） |
| **合并涉及 `server/` 后必须重启 8000** | 前端有 HMR 会自更新，**后端不会**——否则接口 404「没有这个接口」（今天就踩过） |
| 验收工装 | `D:\平台\voice-module`（**不是 git 仓库**）：`汇总验收.py`（51 项矩阵）、`验收界面.mjs`（230 项）、`验收语音包.mjs`、`评估语音.py` 等 |
| 截图/证据 | `D:\平台\voice-module\shots\**`、`voice-module\recordings\reports\**` |
| Python | `D:\平台\voice-module\runtime\python312\python.exe`（先 `$env:PYTHONUTF8="1"`） |
| 环境限制 | **无 puppeteer**（用裸 CDP 写探针）；**无 PIL**；PowerShell 控制台读 UTF-8 会乱码（文件本身正常） |

---

## 1. 已完成（按主题）

### 1.1 语音播报：预录语音包 60 段（ttsmaker，音色「情感 1502 na 娜娜」）

- **机制**：`VoiceOutput.speak()` 优先级 = **预录音频 > speechSynthesis > 静默降级**；键 = **运行时播报文本逐字匹配**（`public/voice/manifest.json`，现 **60 条**文本→音频 + 2 条 `_` 元数据键）。未命中安全回退，绝不"气泡写 A 喇叭念 B"。
- **修掉两个真实故障**（用户报"互动后还是合成音"）：
  1. 气泡自带 `speechSynthesis.speak()` **绕过** `VoiceOutput` → 改为统一走 `VoiceOutput`；
  2. 连问两次时，第二轮 `silenceCurrent()` 清空上一轮 `<audio>` 的 src → 旧元素抛 `Empty src` → **已作废的那一轮**被当成"没放成"又把旧文本合成一遍 → 修法：清 src 前摘 `onended/onerror`；`playAudio.finish()` 代次不匹配时按"已处理"收场。
- 合成工具（在 voice-module，未入库）：`tools/合成-ttsmaker.mjs`（半自动：只差 4 位验证码）、`tools/合并语音包.mjs`（自动接进 manifest）、`tools/导出互动清单.mjs`（从代码渲染文本清单）。
- **重要边界**：带参数的意图只覆盖"默认参数"那一版（`open_page` 只覆盖"任务总览"、`robot_move` 默认 Z04、`load_map` 默认版本），换参数会回退合成音；`open_evidence`/`robot_patrol_route` 共 4 段是 ⛔ 空槽位病句**未录**，这两个意图 100% 合成音。
- ⚠ **ttsmaker 没有账号体系**（首页无登录入口，11 个候选登录地址全 404）；**每单必需人工填图验证码**（`#UserInputCaptcha`），该验证码**不做自动化**（人机校验边界）。**IDM 会截 Chrome 下载，合成前先关掉**；排队弹窗已由脚本自动点（并排除任何付费按钮）。

### 1.2 小木形象（右下角常驻）

- 曾把静态素材 `i04-xiaomu` + 外圈动画改成 **SVG 矢量表情角色**（7 状态各有五官差异 + 双互质周期眨眼 + reduced-motion 下静态可辨）——已提交 `491fa9a`。
- 用户随后提供**新参考图**（蓝色琉璃球）：`D:\平台\voice-module\refs\小木形象参考-蓝色琉璃球体.png`，要求**以照片为最高标准**改成该形象，并且**要足够动态**。
- 已归档的 SVG/CSS 琉璃球候选：**临时分支 `xiaomu-candidate-svg`（`3fbb69f`）**，含气泡外壳拟近平台 token 的改动 + 该候选的 7 状态/reduced-motion 截图（`voice-module/shots/xiaomu-avatar/{before-风格改造,after-风格改造,candidate-A-琉璃球}`）。

### 1.3 RAO 合并（已推 SHI）

- `origin/RAO`（`bafec18`）已合入 SHI：合并提交 **`252e99c`**（129 文件 +30195/−6297），5 处冲突逐条语义解决：
  - `SmallWoodPanel.tsx` / `VoiceConsole.tsx`：两边都留；`WakeOverlay.tsx`：**维持 SHI 的删除**（取证：引用它的 `wakeChannel.ts` 只有 SHI 改过，且其依赖的 css 已删）；
  - `Knowledge.tsx` / `knowledge.css`：以 RAO 版为基线（SHI 旧单体页面 import 了 RAO 已删模块、合并后无法编译），SHI 的 FR-05 引用深链能力**移植**回新页面。
- **同事的未提交串口遗留改动已安全留档**在分支 **`wip-serial-backup-20260914`（`dc43bac`）**，恢复：`git cherry-pick wip-serial-backup-20260914`。用户说那是"早期想用硬件模块做语音识别的遗留"，**目前按"已搁置"处理**（所以 A6-1/A6-1b 两项维持不通过/证据不足）。

### 1.4 验收与评估

- **51 项矩阵最终：通过 45 / 不通过 3 / 证据不足 3**（`docs/验收报告-v2.0.md`、`docs/验收矩阵-v2.0.md`）。
- **A1-8 / A1-9 已转通过**：发现工装把"带唤醒词前缀的整段转写"直接喂 matcher，而产品会先剥唤醒词（真源 `src/pages/MumaiDashboard/agent/wakeChannel.ts:697`）→ 工装改为**运行时从产品源码提取该正则**（不复制正则、不改阈值/判据/样本），**未剥词读数一并保留披露**。
- 仍**不通过**：**A2-1 唤醒漏检 FRR 8.70%（21/23）**、A6-1（COM4 缺失，串口已搁置）。
- 仍**证据不足**：A2-2（真人负样本曝光仅 103.12s，0 误唤醒，上界 104.6 次/小时 > 1）、A2-3b（历史调参污染）、A6-1b。
- 用户已交付并归档的录音：`recordings/raw02`（23 条「小木小木」= 冻结验收集）、`recordings/intents/introduce_platform`（9 条）、`recordings/calib-01`（24 条标定集）、`recordings/negative-real`（38 段 / 93.61s）、**`recordings/accept-sentence`（1 条整句，停顿 1.14s/0.66s 达标）**。

### 1.5 文档

- `docs/小木交互手册-v2.0.md`（669 行，取代 `语音交互操作手册-v1.0.md`）：四个入口、唤醒规则、29 意图交互全集（文本取自生成文件、逐字一致）、录音覆盖与边界、7 状态表情、降级与排队打断、键盘与无障碍、验收现状。
- `docs/语音播报文本清单-v1.0.md` / `语音播报文本汇总-v1.0.txt` / `小木播报文本-合成用-v1.0.txt` / `public/voice/候选键.json`。

### 1.6 已推送的提交（`origin/SHI = 9b63b39`）

```
9b63b39 docs(交互): 小木交互手册 v2.0 + 旧手册取代指引
caef86c docs(验收): 修正工装语义匹配口径（复刻产品剥词）+ 重跑 51 项，A1-8/A1-9 转通过
a7c085b docs(验收): RAO 合并后重跑语音评估与 51 项矩阵
252e99c merge: RAO 合入 SHI
496e2da 语音包补全至 60 段
491fa9a 小木矢量表情形象
7616453 语音包扩到 33 条 / 9b5cd87 ttsmaker 语音包 / e9ac901 外录语音包 + 两处故障修复 / 25bb89f 形象与气泡接管语音交互
```

---

## 2. 进行中：`xiaomu-lab`（three.js 形象方案展台，TDD 工作流）

**用户诉求（原话）**：「单开一个页面做渐变半透明动画。three 绘制，可以带有一定 3D 效果，所有方案多做几版，并排展示之后我会告诉你用哪个，老方法也一起展示，最好全量展示」。

**已拍板的范围**：交付=展台页＋挑完直接落地产品；**每版 ≥30 FPS 硬门槛**（老方法 CSS/SVG 不参与门槛，但必须过"双帧像素差"）；**每版带脸+眨眼**；**自适应含 1366**；同框 9 格（8 WebGL + 1 老方法），上限 12。

### spec 位置与状态

```
tdd-specs/.current → xiaomu-lab
tdd-specs/xiaomu-lab/
  .harness            phase=spec
  discovery.md        10 条已定决策 + 9 版方案清单 + 载体现状
  usecases.draft.md   /tdd:new 的 UC 草稿
  usecases.md         ✅ 6 个 UC + 18 条备选/失败路径 + 逐类路径覆盖（权威）
  requirements.md     ✅ 8 个 REQ（UC→REQ 映射 + EARS）
  design.md           ✅ 模块/接口契约 + 数据流 + 设计决策
  tasks.md            Phase 1: 3 / Phase 2: 25（按 UC 垂直切片）/ Phase 3: 7 E2E
  .loop-evidence.log  RED/GREEN 原始证据（Coder 写入）
```

**进度**：Phase 1 的 1.2/1.3 已 `[x]`（**单测命令 = `node --test src/lab/*.test.ts`**，Node v24.15.0 原生类型剥离，**未新增依赖**；页面 `http://127.0.0.1:5173/xiaomu-lab.html` = HTTP 200）；1.1 及 Phase 2 由 Coder 子代理推进中（12:46 时 registry + layout 测试已过 10/10）。

**素材**：`xiaomu-lab.html` + `src/lab/{types.ts,shared.ts,glassTransmission,jellyWobble,iridescentFlow,doubleShell,plasma,soapFilm,fluidMetaball}.ts` 是早前 spike，**可复用但须对齐 design.md §2 的 `LabVariant`/`VariantHandle` 契约**（`dispose/setPaused/setFocused/renderStats`）；**第 08 版「扁平发光 2.5D」素材未写完**；**第 09 版老方法**要从 `xiaomu-candidate-svg` 分支移植 SVG 琉璃球并补足动画强度。

### ⚠ 新对话接手时第一件事

**精确停止点（2026-09-14 12:52，Coder 被用户暂停，非正常收尾）**：

| 项 | 状态 |
|---|---|
| 单测 | **`node --test src/lab/*.test.ts` → tests 33 / pass 33 / fail 0 / exit 0** ✅（registry / layout / fps 三条纯函数与契约测试已绿） |
| 类型检查 | ❌ **`npx tsc -b` 报错**（半成品：变体正在迁到 `src/lab/variants/` 并对齐新契约） |
| tasks.md | `[x] = 10`、`[~] = 0`、`[ ] = 25`、`[!] = 0` |
| 证据日志 | `tdd-specs/xiaomu-lab/.loop-evidence.log`（7,631 B），末尾记录了 fps 测试一处**合理的断言时序修正**（门槛仍是"连续 3000ms"，只是把喂入时间轴从 4.5s 改成 5.0s，理由写在测试里） |
| 新增文件 | `src/lab/{registry.ts,registry.test.ts,layout.ts,layout.test.ts,fps.ts,fps.test.ts,types.ts}`；变体开始迁入 `src/lab/variants/`（`doubleShell.ts`、`fluidMetaball.ts` 已出现，**与旧契约不兼容**） |

**`tsc -b` 的具体报错（新对话先修这些）**：

```
src/lab/shared.ts(126,39): WebGLRenderTarget 不能赋给 CubeTexture（环境贴图构建方式要改）
src/lab/shared.ts(316,44): 找不到 LinearSRGBColorSpace（three r183 里名字/导入不对）
src/lab/variants/doubleShell.ts(35,31): '../types' 没有导出成员 'VariantSpec'
src/lab/variants/doubleShell.ts(183,7): 'dispose' 不在类型 'VariantRuntime' 上
src/lab/variants/{doubleShell,fluidMetaball}.ts: 声明了 'Stage' 但未使用
```
→ 根因是 Coder 正在**把 spike 变体按 design.md §2 的 `LabVariant`/`VariantHandle` 契约重写**，`types.ts` 与 `variants/*.ts` 处于迁移中间态；`shared.ts` 也正在改造成"真正的宿主（driver + 单一 rAF 循环 + handle 契约）"。

**接手顺序**：

0. **先决定要不要沿用它的半成品**：单测是绿的（registry/layout/fps 可保留），但 `variants/*` 与 `shared.ts` 处于类型不通过的中间态——可直接接着修，也可从 `src/lab/*.ts`（spike 原件，仍在磁盘上）重新按契约收编。
1. 看 `tdd-specs/xiaomu-lab/tasks.md` 的勾选状态 + 上表，判断走到哪；
2. **自己重跑** `node --test src/lab/*.test.ts`、`npx tsc -b`、`npx eslint src/lab` 复核（不要信子代理自述）；
3. 继续时按 tasks.md 顺序（RED → 观察到失败 → GREEN），**未完成不许勾 `[x]`**；
4. 展台完成 → 跑证据工装出「并排大图 + 逐版单图 + 双帧像素差 + `方案对照表.md`（含实测 FPS）」→ **交用户挑选**；
5. 走 `/tdd:e2e`（**必须派独立 Tester，主代理不得读 `commands/e2e.md`**）→ `/tdd:done`；
6. 用户挑选后做 UC-06 落地：换形象本体、**保留 7 状态与 reduced-motion 静态可辨**、`验收界面.mjs` 保持 **230/230**、推 `origin/SHI`。

> 技术备注（Coder 暂停前报告）：`three` 与 spike 模块**在 Node 里可 import**，但需要修正 import 里的文件扩展名——这是让单测能直接断言变体契约的前提。

---

## 3. 待办清单（按优先级）

| # | 事项 | 说明 / 确切命令 |
|---|---|---|
| 1 | **完成 xiaomu-lab 展台**（进行中） | 见 §2；关键指标：9 格同框、每格带脸+眨眼、单一 rAF 循环、列数纯函数、FPS ≥30 |
| 2 | **用户挑选方案** | 交付并排图 + 对照表后等用户拍板（这是 UC-06 的前置） |
| 3 | **UC-06 落地** | 换形象 + 7 状态 + 230/230 + 推 SHI |
| 4 | 唤醒指标收口 | FRR 8.70% 不达标：需补**难例正样本**（小声/远距/侧向/噪声）；A2-3b 需**另起会话**录 20~30 条「小木小木」（`calib-02`） |
| 5 | FAR 收口 | 现 103.12s 曝光不够：需 **≥10 分钟**（含近音词 + 纯底噪），严格统计口径需 ≥3h |
| 6 | FR-01「多条」 | 整句样本再补 **≥4 条**（「小木小木」→停 0.3~1.5s→「查近三个月天气」） |
| 7 | 串口工作去向 | 已留档 `wip-serial-backup-20260914`；要合回来说一声（合回会改变 A6-1/A6-1b 结论） |
| 8 | 可选：`验收键控无障碍.mjs` 与当前代码不符 | 其日志 `[C7]/[C12]` 说明文字引用的是旧实现（退出码 2，在矩阵外） |
| 9 | 可选：服务暴露面 | 5173 监听 `::`、8000 监听 `0.0.0.0`（局域网可访问）；如需收紧可绑 127.0.0.1 |
| 10 | 可选：语音包边界补录 | 若演示会用到具体页面名/柱号/地图版本，可按需补录对应参数版 |

---

## 4. 必须知道的坑（都踩过）

1. **直连 GitHub 超时** → 一切 fetch/push 走 `-c http.proxy=http://127.0.0.1:7890`（一次性，不写配置）。
2. **合并后不重启 8000 → 新接口全 404**。
3. **注入模板字符串里不能出现反引号**（会把字符串截断，踩过两次）；**`\n` 要用 `String.fromCharCode(10)`**；PowerShell 里 `node -e` 会吃掉 `String.raw`。
4. **PowerShell 默认按 GBK 读文件** → 处理中文文本要 `-Encoding UTF8`。
5. **探针判据会过期**：语音包上线后"播报"不再等于"调了 speechSynthesis"（已修 `验收界面.mjs`，改为音频或合成音任一发生即算播报）。
6. **合成时 IDM 会截下载**；ttsmaker 排队弹窗要自动点（但**绝不点分享/同意/升级/付费**）。
7. **`--only` 参数是逗号分隔的单个参数**（`汇总验收.py --only A1-8,A2-2`）。
8. 局部跑矩阵会**覆盖** `recordings/reports/acceptance-v2.json`（全量备份在 `voice-module/tmp-accept-scratch/`）。
9. **裸 CDP 探针**：Chrome 路径探测 + `--remote-debugging-port` + WebSocket；headless 会被 Cloudflare 拦（ttsmaker 场景需 headful）。
10. **`verify --delivery-ready`**：本仓库无 `tdd-specs/.verify/project.md`，`/tdd:done` 走通用检查。

---

## 5. 验收现状数字（可直接引用）

```
51 项矩阵：通过 45 / 不通过 3 / 证据不足 3
  不通过：A2-1 FRR 8.70%（21/23，漏 clip03/clip10）；A6-1（COM4 缺失）
  证据不足：A2-2（FAR，曝光 103.12s）；A2-3b（历史调参污染）；A6-1b
界面验收 验收界面.mjs：230/230
语音包：验收语音包.mjs → manifest 60 条 + audio=1 synth=0
真人命令识别：9/9 = 100%（冻结真集）；TTS 合成命令 5/6（差 1 条缺真值）
唤醒：冻结验收集 21/23；标定集 24/24（只调参、不出指标）；真人负样本 0/38 误唤醒
```
