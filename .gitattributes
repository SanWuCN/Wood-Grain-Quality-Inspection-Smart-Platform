# 统一换行符，避免 Windows 与 macOS/Linux 协作时反复出现 CRLF 警告
* text=auto eol=lf

# Windows 批处理必须保留 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制素材不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ttf binary
*.woff binary
*.woff2 binary
*.docx binary
*.zip binary
*.sog binary

# 用户给的原件按**字节**入库（CRLF 也不动）：这份对照表在测试里用 sha256 钉住，
# 被换行符转换一次哈希就变了，测试会在别人的机器上红
public/photo-batch-20260922/*.csv -text
