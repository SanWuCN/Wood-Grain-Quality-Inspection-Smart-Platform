/**
 * §7 重点改写 · 排练参考（工作清单 v1.0 §7）
 *
 * ── 这一层为什么单独放，不塞进 `script.ts` ──────────────────────────
 * `script.ts` 里存的是**小木对用户指令的应答**（一问一答，用于匹配与播报）。
 * 而 §7 改写的 7 处是**史在剧本里的台词**（或小木对"素材检查"这类过程性播报）——
 * 是小木要"听"的内容，不是小木要说的话。两者混在一个数组里会导致：
 *   · 匹配器把史的台词也当成触发短语；
 *   · 排练者分不清哪句该由平台念、哪句该由人念。
 * 所以单独成表，并**明确标注不进播报**。
 *
 * ── 这 7 处为什么必须改（§7 左列点名的问题）──────────────────────────
 *   · "场景文件网络通道" 容易暗示公网能力 —— 与本次**纯本地**口径直接冲突；
 *   · "小车河通信" 是错字且没有验收口径；
 *   · "高度物联，AI赋能" 空泛、无法核验；
 *   · 段 171 / 333 / 395 与"素材检查"含 `xxxx`/`xxx` 占位符 —— 不能上台。
 *
 * §7 末明确："产品台词保持专业口吻，不把本工作清单中的'喵'带入平台播报。"
 */

export type RehearsalLine = {
  /** 说话人（史的台词为 "史"；过程性播报为 "小木"） */
  speaker: "史" | "小木";
  /** 对应原稿的问题（§7 左列），用于排练时对照"改了什么" */
  replacedIssue: string;
  /** §7 逐字给定的替换后台词 */
  text: string;
};

/**
 * 七处改写，逐字取自 §7 的「替换后的产品台词」列。
 *
 * ⚠ 只作**排练对照**：平台不播报这些句子（它们是史要说的话，
 *   或已由 `script.ts` 对应轮次的固定回答覆盖）。
 */
export const SPEAKER_REWRITES_V1: readonly RehearsalLine[] = Object.freeze([
  {
    speaker: "史",
    replacedIssue: "「场景文件网络通道」容易暗示公网能力",
    text: "场景文件本地通道已经建立，先核对场景包、坐标基准和构件编号。",
  },
  {
    speaker: "史",
    replacedIssue: "「小车河通信」存在错字且没有验收口径",
    text: "小车和平台通信正常，心跳、定位和任务回执都已收到。",
  },
  {
    speaker: "史",
    replacedIssue: "「高度物联，AI赋能」空泛",
    text: "地图、视频流和设备回执已进入同一任务时间轴，接下来按构件编号检查异常。",
  },
  {
    speaker: "史",
    replacedIssue: "段落 171 含 xxxx",
    text:
      "场景已经打开，可以移动视角，也可以按构件编号跳转。现在提交四根木柱的原始关键帧，" +
      "统一使用正视和侧视两类视角，并保留图片编号。我已在场景中完成 Z01 至 Z04 标记。",
  },
  {
    speaker: "史",
    replacedIssue: "段落 333 含模型占位语",
    text:
      "现场适配任务仍在运行。接下来打开同流程的归档验证记录，展示已完成的对照结果，" +
      "两项记录在页面上分开显示。",
  },
  {
    speaker: "史",
    replacedIssue: "段落 395 只有「数据接收完整」",
    text:
      "数据接收完整，雷达420帧、表面图像14帧、端侧结果3份，预处理版本均为comp-v1.4。" +
      "现在进入平台精细分析。",
  },
  {
    speaker: "小木",
    replacedIssue: "素材检查仍有 xxx 占位",
    text:
      "素材检查完成：视频1段，时长4分18秒，分辨率3840×1920，共214个关键帧。" +
      "缺失文件0个，低清晰度片段2处，已在00:43和02:17标记。",
  },
]);

/**
 * §7 左列点名的**旧措辞**：全平台不得再出现（含注释与文档）。
 *
 * 独立导出是为了让"扫描"这件事也能被测试与工装复用 ——
 * §10 阶段 F 要求检查全仓是否仍出现旧措辞。
 *
 * ⚠ 2026-09-17 用户裁定：**「场景文件网络通道」从禁列移除**。
 *   用户当天给的《小木对话总文案.txt》第 4 条，小木的完整回答是
 *   「平台服务可访问，任务已建立。到达现场后我会分别检查小车数据通道、
 *    手持设备通道和**场景文件网络通道**。三类数据使用同一工单编号，
 *    采集时间各自独立记录。」
 *   —— 这是**用户逐字给定要念的台词**，优先于 §7 对措辞的担心（§7 担心它
 *   暗示公网能力）。已向用户确认过保留，不是漏改。
 *   `SPEAKER_REWRITES_V1` 里史的那句替换台词仍然保留（那是史的台词口径）。
 *   所以剩下的禁列是 3 条：小车河 / 高度物联 / AI赋能。
 */
export const FORBIDDEN_LEGACY_PHRASES: readonly string[] = Object.freeze([
  "小车河",
  "高度物联",
  "AI赋能",
]);

/**
 * §7 的替换文案必须出现的关键措辞（用来证明"改写已生效"）。
 * 与 `FORBIDDEN_LEGACY_PHRASES` 配对使用：旧的不出现、新的在。
 */
export const REQUIRED_REWRITE_PHRASES: readonly string[] = Object.freeze([
  "本地通道",
  "心跳",
  "任务时间轴",
  "正视和侧视",
  "归档验证记录",
  "预处理版本",
]);

/**
 * 占位符形态（§10 阶段 F 要求全仓扫描）。
 *
 * ⚠ 抽成常量而不是散落在测试里，是因为**同一个模式有两类用途**，
 *   必须能区分，否则会互相打架：
 *   · **残留**（禁止）：出现在**平台会念的文本**里 —— 台词、触发短语、页面文案；
 *   · **记录**（允许）：出现在 `SPEAKER_REWRITES_V1[].replacedIssue` 里 ——
 *     那一列的作用就是记下"原来这里是占位符"，把它也扫掉等于删掉改写的凭据。
 *   所以扫描函数要能按用途切换，而不是简单 `includes`。
 */
export const PLACEHOLDER_PATTERNS: readonly RegExp[] = Object.freeze([
  /待补文案/g,
  /占位符/g,
  /*
    ⚠ 3 个 x 与 4 个 x 必须**分别精确匹配**，不能用 `includes` 粗判：
      · 原先只收了 `"xxxx"`，而净稿里的实际写法是 **3 个 x**（「xxx这批总时长xxx，视频质量xxx」），
        `includes("xxxx")` 对 3 个 x 恒为 false —— ⑦ 轮台词被过程稿整段覆盖回来，
        占位符守卫却仍是绿灯（2026-09-16 实测漏检）。少一个字符，守卫就整条失效；
      · 反过来，若把 `"xxx"` 也当子串收进去，它又会**吃进** `"xxxx"`，
        于是「这里写着 xxxx」会被报成 `xxx`，改写表里那条记录也跟着判错。
    所以用前后视断言钉住**恰好** 3 个 / 恰好 4 个 x。
  */
  /(?<!x)x{3}(?!x)/g,
  /(?<!x)x{4}(?!x)/g,
]);

/**
 * 扫描一段文本里的占位符。
 *
 * @param text       待扫描文本
 * @param allowRecord true = 允许"记录式"提及（如 `段落 171 含 xxxx`）；
 *                    false = 严格模式，任何出现都算残留
 * @returns 命中的占位符写法（去重，便于直接打进断言消息）
 */
export function findPlaceholders(text: string, allowRecord = false): string[] {
  const hits: string[] = [];
  for (const pattern of PLACEHOLDER_PATTERNS) {
    /* 每条模式都要独立扫全串：一条文本里可能同时出现 3 个 x 与 4 个 x。 */
    for (const hit of text.matchAll(pattern)) {
      const idx = hit.index ?? 0;
      /*
        非严格模式下放行"记录式"提及：形如「含 xxx」「原来是 xxx」这类
        明确在**描述历史问题**的写法。判据是占位符前面 6 个字里出现
        "含 / 原 / 旧 / 曾 / 仍" 这类提示词 —— 与"页面上直接显示占位符"区分开。
      */
      if (allowRecord) {
        const before = text.slice(Math.max(0, idx - 6), idx);
        if (/[含原旧曾仍]/.test(before)) continue;
      }
      if (!hits.includes(hit[0])) hits.push(hit[0]);
    }
  }
  return hits;
}
