/**
 * 剧本快捷键条目表（Ctrl+Q+N → 说出剧本里的某一句话）
 *
 * ── 这份表怎么来的（防幻觉：全部逐字来自剧本）────────────────────────
 * 来源：`木脉智检第二章改.docx` 的 `word/document.xml`（未做任何改写）。
 * 生成链路：
 *   `_script_extract/xiaomu_raw.json`（24 条小木台词 + 触发行，逐字）
 *   → 本文件（每条 = 键位 + 段号 + 触发人 + 触发语）
 * 触发语取的是**该段前面的那一句他人台词**（去掉说话人前缀与括号旁白）。
 *
 * 两条**没有触发语**的条目（剧本标注「无触发语，本地事件/旁白」）：
 *   · 段155（⑪ 轮）—— 小木主动起头的预警；
 *   · 段205（⑮ 轮）—— 等待时段选用的审核清单播报。
 * 它们原本 text 为空，而识别链路会把空文本丢掉 → 现场按键**毫无反应**且无法排查。
 * 现在改用**该段小木自己的台词**当"听到的话"（见 `tools/改-快捷键条目加段号.mjs`），
 * 并按 `roundNo` 直达该轮。
 *
 * ⚠ 每条都带 `roundNo`（段号 → 轮次）：按了哪个键就是确定要看哪一轮，
 *   不允许退回模糊匹配（段203 与段205 的旧触发语逐字相同，匹配器分不出）。
 *
 * ⚠ 键位顺序 = 剧本出场顺序：`1..9` 给前九条，之后 `a..z`（跳过被建单占用的 `l`）。
 * 唯一的例外是**第⑩轮（巡检任务预检）**：它不在新剧本抽取出的 24 场里
 * （它的正文片段在净稿 320 段中不存在，只有旧工作清单 §8 有），所以补在末尾用 `q`，
 * 而不是插进顺序位去顶掉后面所有键 —— 插进去会让清单里所有段的键位全部错位。
 * 改这张表必须同时更新 `_script_extract/*` 里的清单与给用户的那份输出文字清单。
 */
import type { ScriptShortcutEntry } from "./useScriptShortcut";

export const SCRIPT_SHORTCUT_ENTRIES: readonly ScriptShortcutEntry[] = [
  { key: "1", label: "段9·史", roundNo: "①", text: "小木，读取这份工单，解读任务范围和出发清单，生成完整的平台工单。" },
  { key: "2", label: "段13·史", roundNo: "④", text: "已收到指派授权，小木，核对开工清单，显示接下来需要完成的项目。" },
  { key: "3", label: "段15·沈", roundNo: "④", lineOverride: "收到，已启用同步备份", text: "请各岗位报告出发前准备情况。小木，请帮我做同步备份。" },
  { key: "4", label: "段26·沈", roundNo: "②", text: "小木，请你联网查找一下现场情况，提供近三个月的天气数据，评估该地古建可能存在的风险。" },
  { key: "5", label: "段29·沈", roundNo: "③", text: "收到。现场共四根核心古木主体，巡检以 Z01 至 Z04 分别对应四根构件。先完成四柱影像采集以及场景 SLAM 建图，后续再针对存在风险的主体进行精扫。" },
  { key: "6", label: "段58·沈", roundNo: "⑤", text: "小木，给出本次环境补偿参数建议。" },
  { key: "7", label: "段87·史", roundNo: "⑥", text: "小木，检查建图效果和现场视频流，只提醒影响作业的异常。" },
  { key: "8", label: "段101·饶", roundNo: "⑦", text: "小木，检查这批重建素材，列出缺失文件和需要重看的画面。" },
  { key: "9", label: "段122·史", roundNo: "⑧", text: "小木，请比较这四组标记的木构件，按可见异常给出优先复核顺序。请标明依据所在的图片，并说明还不能确定的内容。" },
  { key: "a", label: "段125·史", roundNo: "⑨", text: "小木，打开你标记的原图，把疑点区域放大。" },
  { key: "b", label: "段155·旁白", roundNo: "⑪", text: "Z04当前批次触发适用性预警，异常记录已打开，请架构师确认。" },
  { key: "c", label: "段171·史", roundNo: "⑫", text: "小木，请汇总本次异常证据，并生成补充数据和模型适配的任务清单。" },
  { key: "d", label: "段174·史", roundNo: "⑬", text: "小木，把补采、数据审核和适配验证拆成任务卡，关联本次异常批次。" },
  { key: "e", label: "段188·史", roundNo: "⑭", text: "小木，对照采样计划检查接收清单，缺什么就列什么。" },
  { key: "f", label: "段203·史", roundNo: "⑮", text: "清洗完成，待审核记录已列出，数据集已按物理样本分组。" },
  { key: "g", label: "段205·史", roundNo: "⑮", lineOverride: "需要人工判断的记录已分为重复疑点、采集异常和标签待核验。原文件保持不变，请在列表中确认保留、排除或补采。", text: "需要人工判断的记录已分为重复疑点、采集异常和标签待核验。原文件保持不变，请在列表中确认保留、排除或补采。" },
  { key: "h", label: "段221·史", roundNo: "⑯", lineOverride: "两项记录已分开显示。现场任务按实际进度更新，当前讲解使用归档验证记录；任务状态变化后我再提示。", text: "小木，跟踪现场任务状态，同时打开归档版本的验证摘要。" },
  { key: "i", label: "段229·史", roundNo: "⑯", text: "小木，汇总新旧模型的验证结果，检查部署条件。" },
  { key: "j", label: "段246·史", roundNo: "⑰", text: "平台已收到写入状态，正在等待设备重启和版本回报。" },
  { key: "k", label: "段272·史", roundNo: "⑱", text: "小木，调用本批次分析流程，完成图像标注和雷达分析，再按测区融合结果。" },
  { key: "m", label: "段275·史", roundNo: "⑲", text: "小木，把图像疑点和同测区响应放在一起，列出需要补核的项目。" },
  { key: "n", label: "段291·史", roundNo: "⑳", text: "小木，结合本次检测记录，生成工单草稿，列出复核位置、处理建议和附件。" },
  { key: "o", label: "段299·沈", roundNo: "㉑", text: "小木，生成本次任务复盘，重点写异常原因、处置过程和后续待办。" },
  { key: "p", label: "段313·沈", roundNo: "㉒", text: "小木，根据校验结果整理交付摘要，打开还需要处理的条目。" },
  /*
    第⑩轮（巡检任务预检）没有出现在新剧本抽取出的 24 场里，见文件头说明。
    触发语与轮次台词逐字取自旧工作清单 §8 那一行（`检查巡检任务配置，显示预览后再下发。`），
    不是新编的话。
  */
  { key: "q", label: "⑩ 巡检任务预检", roundNo: "⑩", text: "检查巡检任务配置，显示预览后再下发。" },
];
