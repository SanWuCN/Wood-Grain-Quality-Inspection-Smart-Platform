/**
 * 小木脚本化交互 · 木脉智检第二章
 *
 * ── 这份数据从哪来 ──────────────────────────────────────────────────
 * **当前口径（2026-09-16 起）**：以《新剧本纯本地演示-DSH通宵执行工作清单 v1.0》
 * 的 §6（冻结数据）、§7（重点改写）、§8（触发/台词/可见动作）为准。
 *
 * 历史来源（**已被 v1.0 替代，仅作追溯**）：`4.1木脉智检（第二章）(2).docx`
 * 经《小木交互提取-木脉智检第二章-v1.0.md》提取的 490 段逐段核对稿。
 * 新剧本改用了 `(3).docx`；两者的差异点见 `tools-夜间/RUNLOG-v1.0.md` 的 A4。
 *
 * ⚠ 台词是**冻结文本，不许改写** —— 排演与验收都按 §8 对字。
 *   `scriptLines.test.ts` 会逐轮核对关键数字与措辞。
 *
 * ── 为什么单独一份表，而不是塞进 intents.ts ──────────────────────────
 * `intents.ts` 是**意图目录**（28 条，回答"用户想干什么"，走语义向量匹配）。
 * 这里是**剧本台词**（25 轮，回答"这一轮小木该说哪几句原话"）。
 * 两者的更新节奏完全不同：意图会随产品能力增删，台词是排练/演示时逐字对稿的。
 * 混在一张表里，改意图就会动到台词，改台词又会动到语义匹配的语料。
 *
 * ── 语音编号为什么必须显式写 ────────────────────────────────────────
 * 旧稿里只有几轮带 `（AI语音3）`~`（AI语音8）` 标注（§168/§247/§299/§344/§410/§441）。
 * 现有 `voicePackOf()` 是按**数组下标**推 `AI语音${index+1}` ——
 * 一旦意图表增删，这些编号就会整体漂移，而稿子上的编号是固定的。
 * 所以这里显式声明 `voicePack`，对接语音资源时以它为准。
 *
 * ── 排演约束（稿子文末摘录，实现时必须遵守）──────────────────────────
 * · 未接通真实工具时读**预置演示结果**，且要明确标注"预设标注演示"；
 * · 不虚构放大定位、不创建不存在的证据、不按倒计时编造成功；
 * · 不补写尚未完成的训练成绩。
 * 这些落成每轮的 `demoNote`（该轮未接通时怎么如实说）与 `precondition`（必须先满足什么）。
 */

/** 一轮脚本交互 */
export type ScriptRound = {
  /** 稿子里的圈号，如 "⑧"，用于对稿 */
  roundNo: string;
  /** 段落号，如 "§168–170" */
  paragraph: string;
  /** 幕 */
  act: string;
  /** 这一轮在做什么（一句短标题） */
  title: string;
  /**
   * 触发关键词组 —— 这一轮要匹配的「后文关键词」。
   * 每一项是**独立可选**的触发说法；匹配时不必全中，按覆盖率打分。
   *
   * ⚠ 口径来自工作清单 v1.0 §8 的「语义短语，**不得只匹配单个泛词**」列，
   *   以及 §9.2 的听写变体表。两条硬要求：
   *   · 每条短语**至少 3 个汉字**（§9.2 末：不得把「工单/天气/模型/清单/打开」
   *     这类单个泛词直接映射为动作）；
   *   · 相邻轮的字面要能区分开（例如 ① 必须含「读取+工单」、④ 必须含「开工+清单」 ——
   *     §9.2 点名要消除「打开工单」命中 ④ 的歧义）。
   *   `scriptRounds22.test.ts` 会逐条核对这三件事。
   */
  triggers: string[];
  /**
   * 这一轮的**触发来源**（§8 表第一列）。
   *
   *   "voice"       —— 由唤醒词 + 说法触发（25 轮里的 24 轮）
   *   "local-event" —— **只**由本地任务事件触发，语音不得抢触发（⑪）
   *
   * 为什么要把这件事写成数据而不是靠约定：⑪ 是"小木主动起头"的提醒，
   * 它对应的页面事件是"任务状态进入巡检前检查"。若语音也能触发它，
   * 演示时任何相近说法都会让页面凭空冒出提醒，且与真实因果脱节。
   */
  triggerSource: "voice" | "local-event";
  /** 小木这次要说的话。两句的情况见 ⑮（第 2 句是"等待时选用"） */
  lines: ScriptLine[];
  /** 对接的语音编号；稿子未标注的为 null（走 TTS 或未录制） */
  voicePack: string | null;
  /**
   * 对应哪个既有意图（`intents.ts` 的 id）。
   * 有值表示这一轮除了播台词，还应触发该意图的动作/导航；
   * null 表示纯播报（如 ⑪ 主动预警、③ 等待时段播报）。
   */
  intentId: string | null;
  /**
   * 小木说完之后，**按稿子**该由谁做什么 ——「预期下一动作」。
   *
   * 交互稿的备注里点名要这一列（"编号、触发条件、文本、预期下一动作"），
   * 它也是排练时最实用的一列：念完台词该谁接、接什么，一眼能看到。
   * 文案取自稿子里各轮的"下一句"。
   */
  next?: string;
  /** 这一轮开始前必须先满足的条件（排演约束） */
  precondition?: string;
  /** 交给 TTS 时的语音风格提示 */
  style?: string;
  /**
   * 「随播报逐组展开」的声明（可选）。
   *
   * ── 为什么从"计数"改成"命名"（v1.1）────────────────────────────────
   * 旧写法是 `{ target, panels: 3 }` —— 一个数字，语义只活在
   * `WorkOrderDetail.tsx` 的下标约定里（`stage > 0` / `> 1` / `> 2`）。
   * 客户交接文档《新工单红头委托与小木联动-AI交接文档 v1.0》要求
   * "四组模块**按播报语义节点**依次展开"，而计数无法表达"这一句对应哪一组"，
   * 只能把总时长平均分配 —— 于是"人员 / 环境 / 下发 / 成果"被挤在同一段里
   * 一起冒出来，而文档明令它们**不得提前出现**。
   *
   * ── 两个字段的关系 ──────────────────────────────────────────────────
   * `sections` 是这一轮**允许**揭示的组（顺序即播报顺序，取值见
   * `ordersReveal.ts` 的 `ORDER_DETAIL_SECTIONS`）；
   * `beats[i]` 是**第 i 段台词念完时**该揭示的组。
   * 拍点时间由 `buildRevealSchedule()` 按段累加算出（不是平均分配）。
   */
  reveal?: {
    /**
     * 揭示目标：
     *   · `order-detail`    —— 工单详情页按「组」逐段展开（②③④⑳㉓㉔㉕）；
     *   · `clean-flow`      —— 数据清洗流程页按「阶段」逐拍推进到人工核验（⑰）；
     *   · `workbench-cards` —— 执行工作台的任务卡按「槽位」逐张铺开（⑥⑮，位置编号 c1…c4）；
     *   · `twin-components` —— 三维场景顶部的四柱构件条按「构件号」逐柱点亮（⑪，Z01…Z04）。
     * 四者共用 `ordersReveal.runRevealTimeline` 的时间线，只是应用对象不同。
     */
    target: "order-detail" | "clean-flow" | "workbench-cards" | "twin-components";
    sections: string[];
    beats: string[][];
  };
  /**
   * 页码联动：这一轮说完后页面该**跳到哪**。
   *
   * ── 为什么需要它（实测暴露的缺口）──────────────────────────────────
   * 原先"跳转"是靠**该轮 intent 的 action** 顺带完成的（第①轮正好是
   * `view_current_order` → `open_order`）。但 25 轮里只有少数几轮的 intent
   * 恰好指向工单页：
   *   · ④ ㉑ ㉒ 的 `intentId` 是 `null` —— 根本没有 action 可跑，页面纹丝不动；
   *   · ⑧ ⑩ ⑰ 的动作指向别的页面（构件对比 / 巡检 / 部署检查）；
   *   · ⑳ 跳到了字面量 `?order=draft` —— 占位符没人替换，选中不到任何工单。
   * 逐轮验证时这几条全是"跳转失败"。所以把导航**显式声明出来**，
   * 不再依赖"该轮的意图碰巧是导航类"。
   *
   * ── v2：从"只有工单页"扩到**每一轮都有自己的页面**（用户 2026-09-23 口径）
   * 用户原话：「贴合剧本小木以及我（shi，人工智能架构师）的需要，对平台进行页面添加及
   * 和小木互动时触发的自动操作……操作或展示页面少就添加」。原实现里 `route` 只认
   * `"order"`，于是 25 轮中只有 8 轮会把页面带走，其余 17 轮"念完停在原地、只弹一个小卡片"——
   * 观众看到的是"小木只是回了句话"，而剧本里每一轮都发生在某张页面上
   * （天气档案 / 建图 / 三维场景 / 异常排查 / 数据集 / 训练验证 / 更新交付 / 融合分析…）。
   * 现在 `route` 取平台已有的页面路径，`tab` / `view` / `component` 是页内定位，
   * 由 `tools.ts` 的 `navigate_page` 统一执行（**不新增一套跳转实现**）。
   *
   * ⚠ 路径必须与 `design.ts` 的 `NAV_ITEMS`（一级页面）或页面自身的页签表一致；
   *   `scriptNav.test.ts` 会逐轮核对，写错路径直接红 —— 免得小木把用户带进
   *   「页面不存在」的兜底页（那比不跳还糟）。
   */
  nav?: {
    /**
     * 目标页面：
     *   · `"order"` —— 工单详情（选中某一张，见 `order` 字段）；
     *   · 其余取 `NAV_ITEMS` 的 path（如 `"/hardware"`、`"/firmware"`、`"/twin"`、`"/mapping"`、
     *     `"/knowledge"`、`"/archive"`、`"/"`），走通用的 `navigate_page`。
     */
    route: string;
    /**
     * 选中哪张工单（`route === "order"` 时才有意义）：
     *   "bound"   = **显式绑定优先**（用户点「查看」通知时绑上的那张，防幻觉规则 3）：
     *               绑定时一律以它为准；**未绑定时退回列表最新那张** —— 与台词取值
     *               （`executor.ts` 的 `scriptEntities()`）同一条口径，
     *               免得出现"念的是最新那张工单、页面却停在原地"；
     *   "current" = 列表最新那张（其余轮次的既有口径）；
     *   字符串    = 明确指定。
     */
    order?: "bound" | "current" | string;
    /** 页内页签 key（`/hardware` 与 `/firmware` 用），例如 `env`、`capture`、`dataset`、`fusion` */
    tab?: string;
    /** 页内主视图 key，例如训练验证页的 `compare`（新旧对比） */
    view?: string;
    /** 构件编号，用于数字孪生页（`/twin?component=Z04`） */
    component?: string;
    /** 采集批次号，用于采集页 */
    batch?: string;
  };
};

/**
/** 一句台词 */
export type ScriptLine = {
  text: string;
  /**
   * "main"     = 这一轮的主回复（小木念，唯一会被播报的一句）
   * "waiting"  = 等待时段选用的备用播报（稿子标了"等待时选用"的那些）
   * "audit"    = 人工审核未结束时的备用播报
   * "host"     = **讲解人（操作平台的人）自己说的话**，不是小木的台词。
   *              用户 2026-09-18 对第 4 条的口径：小木只回「收到，已启用同步备份」，
   *              文档里跟在后面的那半句由讲解人自己说 —— 所以它留在剧本里
   *              （当场记与对照表要能看到完整一段），但**不进小木的播报、不合成音频**。
   */
  role: "main" | "waiting" | "audit" | "host";
};

/* ------------------------------------------------------------------ *
 * 剧本（25 轮，按幕顺序）
 * ------------------------------------------------------------------ */

export const SCRIPT_ROUNDS: ScriptRound[] = [
  {
    roundNo: "①",
    paragraph: "（用户 2026-09-17 文档第 1 条）",
    act: "第四幕 · 复扫融合与任务交付",
    title: "三个月巡检与风险统计",
    /* 触发来源：唤醒词 + 说法（快捷键 Ctrl+B+1） */
    triggerSource: "voice",
    triggers: ["到过多少个地方巡检", "发现了多少个风险点", "已修复的有多少", "过去三个月统计"],
    lines: [
      {
        role: "main",
        /*
          ⚠ 用户文档第 1 条（顺序即剧本顺序，所以它在最前）。
          台词里的统计数字（4 处 / 14 个风险点 / 2 处高风险 / 7 处已修复 / 2 处施工中 / 5 处已受理）
          **在冻结数据包里没有出处**（已全仓扫描确认），属"知识库检索"口径的演示内容，
          按用户给定原文逐字入档。
        */
        text: "好的，我正在检索RAG知识库，并核对近期巡检工单、风险记录和施工反馈，请稍候，查询完成。过去三个月共巡检4处地点，发现14个风险点，其中高风险点2处。目前已有7处完成修复，2处正在安排施工，其余5处已受理，正在等待后续处置。",
      },
    ],
    next: "（此轮为统计问答，不接其他岗位台词）",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「我正在检索RAG知识库，并核对近期巡检工单、风险记录和施工反馈」——知识库检索页。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/knowledge",
      tab: "search",
    },
  },
  /* ---------------- 第一幕 · 工单进入与现场部署 ---------------- */
  {
    roundNo: "②",
    paragraph: "§8–10",
    act: "第一幕 · 工单进入与现场部署",
    title: "接单整理",
    /*
      ⚠ 这里原先还有一条 2 字短语「工单」，已去掉。
      触发器精简成短关键词之后（22c2b6f），它是全表唯一的 2 字项，
      而匹配规则**刻意**让 2 字拿不到阈值（可信度 0.6 < 0.62）——
      防的就是"只说两个字就播一整轮"。「工单」又过于泛（"打开工单""工单列表"都会撞），
      留着它只会是一条永远不命中的死条目。同轮已有「读取工单 / 整理任务」够用。
    */
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["读取这份工单", "解读任务范围", "整理出发清单", "读取工单"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "已整理为四项任务：现场建档、风险初筛、重点精扫和复核交付。附件里未明确的信息，我已单独列出，本次任务涉及的木构主体为四根木柱，我已按照z01-z04编号。",
      },
    ],
    next: "沈：收到。我来核对范围。本次完成巡检和辅助诊断，形成可追溯记录。请各岗位报告出发前准备情况。",
    /* 本轮已录音：public/voice/round-01.mp3，manifest 键 = 上面那条主台词逐字 */
    voicePack: "AI语音1",
    /**
     * ⚠ 这一轮原来绑的是 `history_summary`（查历史巡检风险汇总）——
     * 与台词说的「读取这份工单」根本不是一件事，结果是**只播报、不跳转**：
     * 按 Ctrl+Q+L 建单后说「读取这份工单」，页面停在原地不动。
     *
     * 改绑 `view_current_order`（查看当前工单）：它的 action 是 `open_order`。
     */
    intentId: "view_current_order",
    /**
     * 四组模块按播报**语义段**依次展开。
     *
     * `beats[i]` 与正文按标点切出的第 i 段对齐（顺序即播报顺序）：
     *   ① 读取中，工单摘要已生成        → 工单摘要
     *   ② 任务范围和出发清单已生成      → 任务范围与出发清单
     *   ③ 已整理为四项任务…            → 四项任务
     *   ④ 附件里未明确的信息…四柱编号   → 待确认信息 + 检测主体 + 后续执行模块
     *
     * ⚠ 第四组一次带出"待确认 + 主体 + 人员/环境/下发/成果"，是因为文档把
     *   它们归在同一个播报节点下（见交接文档「模块展开节拍」表末行）。
     *   人员在未指派时显示"未指派"、下发保持空态 —— 空态也是确定性事实，不算提前展示。
     */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "tasks", "pending"],
      beats: [["order"], ["scope"], ["tasks"], ["pending"]],
    },
    /** 导航到**显式绑定**的那张工单（不是列表第一条）—— 见防幻觉规则 3 */
    nav: { route: "order", order: "bound" },
  },
  {
    roundNo: "③",
    paragraph: "§58–60",
    act: "第一幕 · 工单进入与现场部署",
    title: "开工清单核对",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    /*
      「已收到指派授权」是用户 2026-09-16 给的**新说法前缀**（史的口播：
      "已收到指派授权，小木，核对开工清单，显示接下来需要完成的项目。"），
      补进来才不会因为多了这半句而失配。原三条触发词保留。

      ⚠ 「同步备份」原也在本轮触发词里（用户说法"小木，请帮我做同步备份。"）。
      2026-09-17 按《小木对话总文案.txt》第 4 条拆成**独立一轮（④ 同步备份）**：
      触发词与回答都搬过去了，本轮不再认这句话。
      （④ 的主台词经用户 2026-09-18 定为「收到，已启用同步备份。」——
        也就是原来段15 那句备用句的原话，只是这次是**小木真的会念**的一句。）
    */
    triggers: ["已收到指派授权", "核对开工清单", "开工检查", "需要完成的项目"],
    lines: [
      {
        role: "main",
        /**
         * 台词**已由用户改写**（2026-09-16，覆盖工作清单 v1.0 §8 的旧摘要）。
         *
         * 旧句：「开工清单已核对，设备、人员和资料三类共12项，11项就绪，1项待现场确认。」
         * 新句：「收到，我来核对范围，本次完成巡检和辅助诊断，形成可追溯记录。」
         *
         * 为什么换：用户给的对话是**受领指派后先确认范围**（巡检 + 辅助诊断 + 可追溯记录），
         * 而不是报清单统计数。旧句里的 12/11/1 三个数字是清单口径，与本轮语义不符。
         * ⚠ 工作清单 v1.0 §8 那一行仍是旧文本 —— 以本条注释为准，别按旧清单"改回去"。
         */
        text: "收到，我来核对范围，本次完成巡检和辅助诊断，形成可追溯记录。",
      },
    ],
    next: "沈：现在开始执行任务。具身智能工程师做好小车建图和全景相机录制准备。",
    /* 本轮已录音：public/voice/round-04.mp3（AI语音2：稿子上 1/3/4… 之间空着的那号） */
    voicePack: "AI语音2",
    /*
     * 这一轮讲的正是工单详情里的东西 → 让详情跟着台词逐段展开。
     * 数量 3 = WorkOrderDetail 的三个可揭示分区（摘要 / 指派 / 环境·下发）。
     */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "pending"],
      /* 三段节拍：摘要 → 任务范围 → 后续执行模块（与 `ordersReveal.ts` 的组名对齐） */
      beats: [["order"], ["scope"], ["pending"]],
    },
    nav: { route: "order", order: "current" },
    intentId: null,
  },
  {
    roundNo: "④",
    paragraph: "（用户 2026-09-17 文档第 4 条）",
    act: "第一幕 · 工单进入与现场部署",
    title: "同步备份",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    /*
      「同步备份」原来挂在 ③（开工清单核对）的触发词里，回答用的是稿子段15
      「收到，已启用同步备份」（备用句，一行带过）。用户 2026-09-17 的
      《小木对话总文案.txt》第 4 条给了**完整回答**，于是拆成独立一轮：
      触发词从 ③ 移到这里。

      ── 用户 2026-09-18 的最终口径（覆盖文档第 4 条的切分）──────────────
      「这个对话应该回复的是小木：收到，已启用同步备份，然后展开一个动态备份窗口做显示，
        后面那些平台服务可访问什么的是我自己说的」。

      所以这一轮现在是**一句小木 + 一句讲解人**：
        · 小木（`role: "main"`，会被播报、有音频）：「收到，已启用同步备份」；
        · 讲解人（`role: "host"`，不播报、不合成音频）：文档里原来跟在小木后面的
          「平台服务可访问，任务已建立。到达现场后我会分别检查小车数据通道、
           手持设备通道和场景文件网络通道。三类数据使用同一工单编号，采集时间各自独立记录。」
          逐字保留在下面那条 host 行里，供对照表与排练查看（删掉就查无此句了）。
        · 说完之后弹出「同步备份小窗」：`executor` 按 `roundNo === "④"` 派发
          `mumai:sync-backup`，小窗里逐条滚动列出真实备份对象（见 syncBackup.ts）。
    */
    triggers: ["同步备份", "请帮我做同步备份"],
    lines: [
      {
        role: "main",
        /*
          ⚠ 用户 2026-09-18 原话：「这个对话应该回复的是小木：收到，已启用同步备份」。
          句号照旧带上（与其它轮的口播一致）；音频见 `public/voice/round-04.mp3`
          （清单键 = 这句逐字），时长比原来那句长的短很多 —— 小窗的弹出时机是
          "等播报 Promise resolve"，所以念完就弹，不会提前关。
        */
        text: "收到，已启用同步备份。",
      },
      {
        role: "host",
        /* 讲解人自己说的一段（文档第 4 条后半句，逐字保留；不播报、不合成音频） */
        text: "平台服务可访问，任务已建立。到达现场后我会分别检查小车数据通道、手持设备通道和场景文件网络通道。三类数据使用同一工单编号，采集时间各自独立记录。",
      },
    ],
    next: "（小木说完弹出同步备份小窗，列出真实备份对象；随后的通道说明由讲解人自己讲，见 syncBackup.ts）",
    /*
      ⚠ 2026-09-18 音频状态：**这一句还没录**（ttsmaker 合成要过墙，见 docs 里那份 25 条清单）。
      句短、改过词，旧录音（`round-04.mp3`，念的是讲解人那半句）已删 —— 留着它会
      "气泡写新句、喇叭放旧句"，比没有录音更糟。当前走平台自带的回退：清单里没有键
      → 浏览器合成音念**正确的新词**（`manifest.json` 的「没命中的句子自动回退」那条）。
      合成好新音频后：文件放到 `public/voice/round-04.mp3`，
      再 `node tools-夜间/同步语音清单.mjs --write` 补键，最后 `npm run build`。
    */
    voicePack: null,
    /*
      页面动作：与 ③ 同一张工单详情页，按播报把「摘要 / 任务范围 / 后续执行模块」
      依次点亮（三段台词 = 三拍）。这一段讲的是"任务已建立、三类数据通道分别检查"，
      对应的正是工单详情里的摘要、范围与下发区。
      ⚠ 声明里不能出现排不进任何一拍的组（`ordersReveal.test.ts` 会逐轮核对）。
    */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "pending"],
      beats: [["order"], ["scope"], ["pending"]],
    },
    nav: { route: "order", order: "current" },
    intentId: null,
  },
  {
    roundNo: "⑤",
    paragraph: "§25–27",
    act: "第一幕 · 工单进入与现场部署",
    title: "天气风险查询",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["近三个月天气", "天气数据", "古建风险", "现场情况"],
    lines: [
      {
        role: "main",
        /*
          ── 用户口径（2026-09-17）──────────────────────────────────────
          「第5个对话，天气那个，小木的回答带上较为真实的数据，与平台不穿帮，
            没音频去网站合成」。

          所以这句话 = **用户文档第 5 条的原句打头**（「已按工单地点建立天气查询。」，
          逐字保留，方便与文档对稿）+ **平台自己的天气数据**（不能自己编：
          屏幕右侧同时开着「平台环境档案」面板，台词里的每个数字都必须能在面板上
          原样找到，否则观众一眼就看到两处对不上）。

          数字与来源（全部取自 `DEMO_SCENARIO_V3.weather`，见 seed/scenario.ts）：
            · 412   ← weather.rain.totalMm（面板「累计降雨 412 mm」）
            · 37    ← weather.rain.rainyDays（「降雨天数 37 天」）
            · 52.6  ← weather.rain.peakDailyMm（「单日降雨峰值 52.6 mm」）
            · 78    ← weather.humidity.avgPct（「平均相对湿度 78 %」）
            · 17.8  ← weather.wind.maxGustMs（「最大阵风 17.8 m/s」）
            · 近三个月 ← rangeStart 2026-06-11 → rangeEnd 2026-09-10
            · 四个检查项 ← rain.risks[柱脚积水返潮/屋面排水] + humidity.risks[漆层起翘]
              + wind.risks[迎风面连接]
          `scriptLines.test.ts` 会逐项核对"台词里的数字与数据包同源"，
          以后谁把数据包改了却忘了改这句台词，测试直接红。

          ⚠ 这里是唯一一处**主台词长于用户文档原句**的轮次（文档只给了第一句）。
          生成器 `tools-夜间/出快捷键条目.mjs` 因此把"文档答句 == 轮次台词"的判据
          放宽成"前者必须是后者的**前缀**"，并会把这类扩展逐条打印出来 ——
          是明面允许的扩展，不是静默走样。

          ⚠ 句子数：`splitSegments` 只按 。！？；切，本轮没有 `reveal` 声明，
          段数不影响任何揭示节拍，所以扩成四句是安全的。
        */
        text:
          "已按工单地点建立天气查询。" +
          "近三个月累计降雨412毫米、降雨37天、单日峰值52.6毫米，" +
          "平均相对湿度78%，最大阵风17.8米每秒。" +
          "建议优先检查柱脚积水返潮、屋面排水、漆层起翘和迎风面连接；现场结论以实测为准。",
      },
    ],
    next: "沈：收到。现场共四根核心古木主体，巡检以 Z01 至 Z04 分别对应四根构件。",
    /*
      本轮已录音：public/voice/round-05.mp3。
      ⚠ 2026-09-17 台词加数据后**必须重新合成**（旧文件只念了第一句），
      合成清单与命令见 `docs/语音播报文本汇总-第二章25条-v2.0.txt`；
      合成完跑 `tools-夜间/同步语音清单.mjs --write --prune` 把键补上、
      并把指向这个文件的历史旧键清掉（否则"键写的是一句、放的是另一句"）。
      与 `site_weather` **意图**那条不是同一句话：意图说的是归档天气档案口径，
      本轮是剧本口径的整句播报 —— 两条各自有键、各自命中，互不覆盖。
    */
    voicePack: "AI语音9",
    intentId: "site_weather",
    /*
      页面落点（v2 小木带路）：「已按工单地点建立天气查询」——环境记录页（近三个月天气档案 + 该地风险提示）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "env",
    },
  },
  {
    roundNo: "⑥",
    paragraph: "§30–32",
    act: "第一幕 · 工单进入与现场部署",
    title: "任务顺序同步",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["同步工单任务", "同步到工作台"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        // ⚠ 待人工确认：括号内容是否上屏（按逐字口径保留在 text 里）
        text: "我已把工单任务同步到工作台。环境配置、地图、场景和检测批次将关联本次工单，交接时可直接查看上一岗位提交的结果。",
      },
      {
        role: "waiting",
        /* 备用播报：工作清单未替换该句，原样保留；只在对应时机播，不主动念 */
        text: "进场搬运尚未结束时由小木播报，队员继续整理装备。",
      },
    ],
    next: "（等待时选用）进场搬运尚未结束时由小木播报，队员继续整理装备。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「我已把工单任务同步到工作台」——**执行工作台**（/workbench，任务卡页面）。
      这一页与 /present、/console 同一种做法：有路由、不进一级导航（PRD 2.2 固定八项），
      由小木带路与工单页入口进入；⑥ 说完由 executor 幂等生成「开工四项」草稿。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/workbench",
    },
    /*
      逐张铺开（`workbenchReveal.ts`）：⑥ 两句话 → 先说"已同步到工作台"亮前两张，
      再说"环境配置、地图、场景和检测批次将关联本次工单"时亮后两张。
      与工单页的逐组展开共用 `runRevealTimeline`，只是目标是工作台的卡片槽位（位置编号 c1…c4）。
    */
    reveal: {
      target: "workbench-cards",
      sections: ["c1", "c2", "c3", "c4"],
      beats: [["c1", "c2"], ["c3", "c4"]],
    },
    precondition: "操作者先说「现场共四根核心古木主体…」（本轮的上一句）",
  },
  {
    roundNo: "⑦",
    paragraph: "（用户 2026-09-17 文档第 7 条）",
    act: "第一幕 · 工单进入与现场部署",
    title: "设备编号与数据来源核对",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["同步记录过程", "核对数据来源", "不串数据"],
    lines: [
      {
        role: "main",
        /* 台词逐字来自用户《木脉智检第二章改.docx》第 7 条，不改字 */
        text: "收到。我按设备编号核对数据来源，确认平台显示的是本次设备数据，不串数据。发现异常立即叫停。",
      },
    ],
    next: "（本轮为新增的设备编号核对，暂未录音）",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「我按设备编号核对数据来源，确认平台显示的是本次设备数据，不串数据」——**数据接收**（三路通道 + 本单接收清单）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "receive",
    },
  },
  {
    roundNo: "⑧",
    paragraph: "§75–77",
    act: "第一幕 · 工单进入与现场部署",
    title: "补偿参数建议",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    /*
      「参数推荐」是用户 2026-09-16 给的说法（沈：`小木，给出本次环境补偿参数建议。`
      已在原触发词里）；补「参数推荐」是因为用户把这一轮的动作描述成
      "调用配置查询和参数推荐工具"，现场也可能这么说。
    */
    triggers: ["环境补偿", "补偿参数建议", "参数推荐", "参数建议"],
    lines: [
      {
        role: "main",
        /**
         * 台词**已由用户改写**（2026-09-16，覆盖工作清单 v1.0 §8 的旧摘要）。
         *
         * 旧句：「已按26.4摄氏度、78%湿度和1.6米每秒风速生成CFG-02建议，需现场确认后应用。」
         * 新句：三段 —— 生成对照表 / 采用口径与待核验 / 交谁复核再下发。
         *
         * ⚠ 数字必须留在句子里（26.4、78%、1.6、CFG-02）：
         *   · 它们取自 `CONFIG_DIFF` 与 `ENV_RECORD` 的真实工况，是这一轮的"具体数据"；
         *   · `scriptLines.test.ts` 会核对本轮台词必须含这些片段，抽空了就红。
         * ⚠ 「请全栈工程师用参考件复核后下发」是本轮的**可见动作口径**：
         *   只生成建议、不自动改写设备参数（与工作清单 §8 动作列一致）。
         */
        text:
          "我已生成参数对照表。" +
          "建议优先采用与当前材种和采集条件匹配的标定配置；" +
          "没有对应记录的项目保留待核验。请全栈工程师用参考件复核后下发。",
      },
    ],
    next: "饶：收到，正在根据环境数据调整补偿参数。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「我已生成参数对照表……请全栈工程师用参考件复核后下发」——环境记录页的参数建议对照。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "env",
    },
  },
  /* ---------------- 第二幕 · 建图重建与风险初筛 ---------------- */
  {
    roundNo: "⑨",
    paragraph: "§119–120",
    act: "第二幕 · 建图重建与风险初筛",
    title: "地图/视频通道巡查",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["检查建图和视频", "影响作业的异常", "现场视频流"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "已开启通道巡查，我会先查看当前建图效果，然后通过小车视频流分析现场情况。",
      },
    ],
    next: "马：我现在沿四根木柱外侧缓慢移动，再回到已走过的区域。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「已开启通道巡查，我会先查看当前建图效果，然后通过小车视频流分析现场情况」——建图巡航（建图 + 相机流）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/mapping",
    },
  },
  {
    roundNo: "⑩",
    paragraph: "§139–141",
    act: "第二幕 · 建图重建与风险初筛",
    title: "重建素材检查",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["检查重建素材", "缺失文件", "重看画面"],
    lines: [
      {
        role: "main",
        /*
          台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7）。
          ⚠ 净稿段101 的原文是「正在检查素材。。（3s），xxx这批总时长xxx，视频质量xxx。」
          —— 那是**带舞台提示与占位符的过程稿**，不能上屏也不能上麦（§7 左列点名"不能上台"）。
          §7 右列已逐字给出替换后的产品台词，就是下面这句；映射表 v1.0 也把 ⑦ 判为
          「已实现-文案不同（=该定稿句）」。按新剧本改台词时这句曾被过程稿整段覆盖回来，
          由占位符守卫抓回，这里保留定稿句作为唯一口径。
        */
        text: "正在检查素材，素材检查完成：视频1段，分辨率3840×1920，共214个关键帧。缺失文件0个，低清晰度片段2处，已在00:43和02:17标记。",
      },
    ],
    next: "饶：我们使用 MipMap 软件进行全景影像的高斯场景重建。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「检查这批重建素材，列出缺失文件和需要重看的画面」——**素材质检**（/materials）。
      这一页与 /workbench 同一种做法：有路由、不进一级导航（PRD 2.2 固定八项），
      由小木带路与数字孪生页「场景版本」右上角的入口进入。
      路径由 `scriptNav.test.ts` 对着 routes.tsx 核对。
    */
    nav: {
      route: "/materials",
    },
  },
  {
    roundNo: "⑪",
    paragraph: "§168–170",
    act: "第二幕 · 建图重建与风险初筛",
    title: "四柱风险初筛",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["对比四根木柱", "按风险排序", "检测结果排序", "比较四根木柱", "四根木柱", "比较四组木构件"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "当前Z04视角可见较明显的表面缺损和孔洞状疑点，建议优先复核Z04下部测区。图像可以提示外观异常，不能确认内部是否存在空洞，也不能直接判定承载能力。建议补充近距离图像和手持检测数据。",
      },
    ],
    next: "史：小木，打开你标记的原图，把疑点区域放大。",
    /*
      ── ⑪ 的页面落点与揭示目标（2026-09-23 改）────────────────────────────
      剧本这一轮演在**三维场景**里：史「现在提交四根木柱对应的原始关键帧……
      （在三维场景给木柱打标签）」→ 小木报出「当前 Z04 视角可见较明显的表面缺损和
      孔洞状疑点，建议优先复核 Z04 下部测区」→ 史「我现在跳转到 Z04 观察视角」。
      原先把页面带到工单详情（按组展开）—— 场地对不上，而用户口径是"每一轮都切到
      剧本对应的真实页面"。现在改成 /twin，并让顶部的**四柱构件条逐柱点亮**：
        第 1 句（比较四组标记构件后的结论）→ Z01/Z02/Z03 就位；
        第 2 句（建议优先复核 Z04 下部测区）→ Z04 点亮，同时打出「建议优先复核」；
        第 3、4 句是对结论的限定与补充建议，Z04 保持点亮。
    */
    reveal: {
      target: "twin-components",
      sections: ["Z01", "Z02", "Z03", "Z04"],
      beats: [["Z01", "Z02", "Z03"], ["Z04"], ["Z04"], ["Z04"]],
    },
    /* 三维场景是要"看"的页：构件号一起带过去，落地就选中 Z04 的视角与热点详情 */
    nav: { route: "/twin", component: "Z04" },
    voicePack: "AI语音3",
    intentId: "compare_columns",
    precondition: "真实视觉模型未接通时，结果须明确标注为「预置标注记录」",
  },
  {
    roundNo: "⑫",
    paragraph: "§171–173",
    act: "第二幕 · 建图重建与风险初筛",
    title: "打开标注原图",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["打开Z04证据", "标记原图", "原始证据"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "对应原图已打开，标注与构件编号一起显示。请核对这处表面缺损。",
      },
    ],
    next: "史：报告项目经理，平台建议优先复核 Z04 下部。",
    voicePack: null,
    intentId: "open_evidence",
    /*
      页面落点（v2 小木带路）：「打开你标记的原图，把疑点区域放大」——数字孪生（构件热点与原始证据）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/twin",
    },
    precondition: "不得擅自放大定位",
  },
  /* ---------------- 第三幕 · 异常拒判与模型更新 ---------------- */
  {
    roundNo: "⑬",
    paragraph: "§230",
    act: "第三幕 · 异常拒判与模型更新",
    title: "巡检前检查（小木主动发起）",
    /* 触发来源：本地任务事件（语音不得抢触发） */
    triggerSource: "local-event",
    triggers: [],    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "Z04当前批次触发适用性预警，异常记录已打开，请架构师确认。",
      },
    ],
    next: "史：全栈开发工程师，请暂停当前采集，保留设备位置和这批原始数据！",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「Z04当前批次触发适用性预警，异常记录已打开，请架构师确认」——异常排查。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "triage",
    },
    precondition:
      "**唯一由小木主动起头的一轮**（工作清单 §8 明文：不接受语音抢触发）。" +
      "触发来源是本地任务事件「任务状态进入巡检前检查」—— 即任务推进到该节点时由平台自动播报，" +
      "不靠语音唤醒进入；`triggerSource: \"local-event\"` 与空 `triggers` 一起把这条约束写成数据。",
  },
  {
    roundNo: "⑭",
    paragraph: "§247–249",
    act: "第三幕 · 异常拒判与模型更新",
    title: "异常证据汇总",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["汇总采集异常", "有证据的异常"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "建议核对材种来源与标定范围，补充有来源的参考样本，检查数据质量，并验证候选模型。适配结果通过独立验证和设备端检查前，保留待复核状态。",
      },
    ],
    next: "史：小木，把补采、数据审核和适配验证拆成任务卡，关联本次异常批次。",
    voicePack: "AI语音4",
    intentId: "anomaly_summary",
    /*
      页面落点（v2 小木带路）：「汇总本次异常证据，并生成补充数据和模型适配的任务清单」——异常排查（证据与结论栏）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "triage",
    },
  },
  {
    roundNo: "⑮",
    paragraph: "§250–252",
    act: "第三幕 · 异常拒判与模型更新",
    title: "任务卡拆分",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["拆分异常任务", "生成任务卡"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "任务卡已生成。补采交全栈执行，样本与测区由具身核对，项目经理审核分组和验证结果，平台记录各项回执。",
      },
    ],
    next: "沈：启动适配流程。只用提前授权、来源明确的参考样本。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「任务卡已生成。补采交全栈执行，样本与测区由具身核对」——**执行工作台**
      （/workbench）；⑮ 说完由 executor 幂等生成「异常适配四项」草稿，保存与回执留在页面上由人点。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/workbench",
    },
    /*
      ⑮ 的台词一口气点了四张卡的分工 —— 用**小句**切段（逗号也切）正好一句一张：
      「补采交全栈执行」→ 第 1 张、「样本与测区由具身核对」→ 第 2 张、
      「项目经理审核分组和验证结果」→ 第 3 张、「平台记录各项回执」→ 第 4 张。
      第一句「任务卡已生成」不点亮任何卡（那一拍是"卡还没出来"的语义）。
    */
    reveal: {
      target: "workbench-cards",
      sections: ["c1", "c2", "c3", "c4"],
      beats: [[], ["c1"], ["c2"], ["c3"], ["c4"]],
    },
  },
  {
    roundNo: "⑯",
    paragraph: "§276–277",
    act: "第三幕 · 异常拒判与模型更新",
    title: "采样计划与接收清单核对",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["核对接收清单", "缺了什么"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        // ⚠ 待人工确认：括号内容是否上屏（按逐字口径保留在 text 里）
        text: "我正在按样本编号核对文件和路径记录。缺失项会保留在补采清单中，已接收文件不会重复要求上传。",
      },
    ],
    next: "饶：本轮样本采集结束。原始数据包已提交。",
    voicePack: null,
    intentId: null,
    /*
      页面落点（v2 小木带路）：「对照采样计划检查接收清单，把缺的列出」——**数据接收**（按样本编号核对的路径记录与补采清单）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/hardware",
      tab: "receive",
    },
  },
  {
    roundNo: "⑰",
    paragraph: "§299–303",
    act: "第三幕 · 异常拒判与模型更新",
    title: "数据清洗与人工审核",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["清洗这批数据", "物理样本分组", "数据清洗"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "清洗完成，待审核记录已列出，数据集已按物理样本分组。",
      },
      {
        role: "audit",
        /* 备用播报：工作清单未替换该句，原样保留；只在对应时机播，不主动念 */
        text: "需要人工判断的记录已分为重复疑点、采集异常和标签待核验。原文件保持不变，请在列表中确认保留、排除或补采。",
      },
    ],
    next: "沈：我来检查数据划分。同一块木样的连续扫描很相似。",
    voicePack: "AI语音5",
    intentId: "clean_dataset",
    /*
      页面落点（v2 小木带路）：「启动数据清洗，列出需要人工审核的记录，生成数据集划分」——数据集（清洗流程逐拍推进）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/firmware",
      tab: "dataset",
    },
    /*
      数据清洗流程：按台词的三小句逐拍推进（选择数据集 → 配置阈值 → 预检查 → 执行清洗）。
      ⚠ 只推到 `cleaned`：**人工核验一步不替人点** —— 核验是人的责任，
        要逐条点采纳/排除（平台口径：这一步是人工责任，必须逐条过）。
    */
    reveal: {
      target: "clean-flow",
      sections: ["pick", "configure", "precheck", "cleaned"],
      beats: [["pick"], ["configure"], ["precheck"], ["cleaned"]],
    },
    precondition: "第 2 句是人工审核未结束时的备用播报，只在审核未完成时播",
  },
  {
    roundNo: "⑱",
    paragraph: "§344–346",
    act: "第三幕 · 异常拒判与模型更新",
    title: "归档验证摘要",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["跟踪现场任务状态", "归档版本的验证摘要", "两项记录"],
    lines: [
      {
        role: "main",
        text: "两项记录已分开显示。现场任务按实际进度更新，当前讲解使用归档验证记录；任务状态变化后我再提示。",
      },
    ],
    next: "史：本次部署使用屏幕上的归档版本，准备执行量化与封装。",
    voicePack: "AI语音6",
    intentId: null,
    /*
      页面落点（v2 小木带路）：「跟踪现场任务状态，同时打开归档版本的验证摘要」——训练验证。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/firmware",
      tab: "training",
    },
    precondition: "现场任务仍在进行，讲解走归档验证记录，不把归档说成现场已完成",
  },
  {
    roundNo: "⑲",
    paragraph: "§344–346",
    act: "第三幕 · 异常拒判与模型更新",
    title: "新旧模型验证汇总",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["对比两个模型", "固定测试集", "新旧模型"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "验证对照已打开。该归档版本通过离线验证，可以进入设备部署检查。",
      },
    ],
    next: "史：本次部署使用屏幕上的归档版本，准备执行量化与封装。",
    /*
      ⚠ 编号为空是**故意的**：稿子把 §344–346 整段标了一个「AI语音6」，
      而这一段后来被拆成两轮（⑱ 归档验证摘要 / ⑲ 新旧模型验证汇总）——
      一个编号没法同时属于两轮，重复挂会让"对接音频时按编号找文件"张冠李戴
      （`scriptVoicePack.test.ts` 直接查重）。
      编号只是对接旧稿音频时的人读标注，不是播放凭据：
      真正决定"播哪个文件"的是 `public/voice/manifest.json`（键 = 逐字台词），
      本轮的录音是 `public/voice/round-19.mp3`，已在清单里逐字命中。
      所以 ⑱ 保留「AI语音6」作为该段旧稿编号的存档，⑲ 留空。
    */
    voicePack: null,
    intentId: "compare_models",
    /*
      页面落点（v2 小木带路）：「汇总新旧模型的验证结果，检查部署条件」——训练验证的新旧对比。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/firmware",
      tab: "training",
      view: "compare",
    },
    precondition: "不补写尚未完成的训练成绩",
  },
  {
    roundNo: "⑳",
    paragraph: "§372–373",
    act: "第三幕 · 异常拒判与模型更新",
    title: "设备版本回报核对",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["核对目标版本", "设备版本回报"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "我正在核对目标版本与设备回报。收到版本信息后还要检查自检结果，两项一致才会更新交付状态。",
      },
    ],
    next: "饶：更新完成，版本核对一致，参考输入检查通过。",
    /*
      ── ⑳ 的页面落点（2026-09-23 改）────────────────────────────────────
      剧本这一轮是【S18《设备更新与回验》】的等待段：「平台已收到写入状态，正在等待
      设备重启和版本回报」，小木「我正在核对目标版本与设备回报。收到版本信息后还要
      检查自检结果，两项一致才会更新交付状态」。
      对得上的页面是**固件及模型 · 更新交付**：交付包、下载与接收、以及「回验与取用记录」
      （版本回执 + 自检结论）都在那一屏；原先把页面带到工单详情（下发区），
      与剧本里的 B 屏（全栈电脑那一路）对不上。
      ⚠ 不再声明 reveal：那一页的"逐块展开"没有语义（这一轮是**等待**，不是逐项讲解），
        所以这一轮回到普通浮层（`demoActions.ts` 里的 `revealOnly` 一并去掉）。
    */
    nav: { route: "/firmware", tab: "delivery" },
    voicePack: null,
    intentId: "deployment_check",
    precondition: "不按倒计时编造成功",
  },
  /* ---------------- 第四幕 · 复扫融合与任务交付 ---------------- */
  {
    roundNo: "㉑",
    paragraph: "§410–412",
    act: "第四幕 · 复扫融合与任务交付",
    title: "本批次分析流程",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["精细分析", "雷达图像融合"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "分析完成，图像标注与雷达结果已关联到Z04测区，融合视图已生成。",
      },
    ],
    next: "史：小木，把图像疑点和同测区响应放在一起，列出需要补核的项目。",
    voicePack: "AI语音7",
    intentId: "run_fusion",
    /*
      页面落点（v2 小木带路）：「调用本批次分析流程，完成图像标注和雷达分析，再按测区融合结果」——融合分析。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/firmware",
      tab: "fusion",
    },
  },
  {
    roundNo: "㉒",
    paragraph: "§413–415",
    act: "第四幕 · 复扫融合与任务交付",
    title: "证据对照与补核清单",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["打开证据对照", "可靠疑点", "待补采"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "证据对照已打开。两路共同提示的项目优先展示，结果不一致或资料不齐的项目已列入补核清单。",
      },
    ],
    next: "史：多模态融合不能直接把两个置信度相加。",
    voicePack: null,
    intentId: "open_evidence",
    /*
      页面落点（v2 小木带路）：「把图像疑点和同测区响应放在一起，列出需要补核的项目」——数字孪生（构件与测区证据对照）。
      路径与页签 key 由 `scriptNav.test.ts` 对着页面自身的页签表核对。
    */
    nav: {
      route: "/twin",
    },
  },
  {
    roundNo: "㉓",
    paragraph: "§441–443",
    act: "第四幕 · 复扫融合与任务交付",
    title: "工单草稿生成",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["生成工单草稿", "根据当前证据", "工单草稿"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "工单草稿已生成。Z04下部已列为重点复核项，检测图像和雷达分析已附上，处理建议待专业审核。",
      },
    ],
    next: "沈：工单里要写清楚后续责任人。持续受潮的区域先排查积水、排水和渗漏源头。",
    /*
     * 这一轮讲的正是工单详情里的东西 → 让详情跟着台词逐段展开。
     * 数量 3 = WorkOrderDetail 的三个可揭示分区（摘要 / 指派 / 环境·下发）。
     */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "pending"],
      /* 三段节拍：摘要 → 任务范围 → 后续执行模块（与 `ordersReveal.ts` 的组名对齐） */
      beats: [["order"], ["scope"], ["pending"]],
    },
    nav: { route: "order", order: "current" },
    voicePack: "AI语音8",
    intentId: "draft_workorder",
  },
  {
    roundNo: "㉔",
    paragraph: "§454–456",
    act: "第四幕 · 复扫融合与任务交付",
    title: "任务复盘生成",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["生成任务复盘", "已经验证", "仍待处理"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "复盘草稿已生成，分为任务完成情况、异常处置、版本交付和后续待办。未完成事项单独列出，项目经理可直接修改后纳入报告。",
      },
    ],
    next: "史：我们将工单状态分为待复核、待处理、处理中和待验收。",
    /*
     * 这一轮讲的正是工单详情里的东西 → 让详情跟着台词逐段展开。
     * 数量 3 = WorkOrderDetail 的三个可揭示分区（摘要 / 指派 / 环境·下发）。
     */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "pending"],
      /* 三段节拍：摘要 → 任务范围 → 后续执行模块（与 `ordersReveal.ts` 的组名对齐） */
      beats: [["order"], ["scope"], ["pending"]],
    },
    nav: { route: "order", order: "current" },
    voicePack: null,
    intentId: null,
  },
  {
    roundNo: "㉕",
    paragraph: "§476–478",
    act: "第四幕 · 复扫融合与任务交付",
    title: "交付摘要整理",
    /* 触发来源：唤醒词 + 说法 */
    triggerSource: "voice",
    triggers: ["整理交付摘要", "打开待处理条目"],
    lines: [
      {
        role: "main",
        /* 台词逐字冻结：工作清单 v1.0 §8「小木固定回答摘要」（数据取自 §6，措辞按 §7） */
        text: "交付摘要已更新。文件校验结果和待办清单分别列出，复盘草稿已关联本次工单，等待项目经理审核。",
      },
    ],
    next: "沈：本次待办事项是否已登记？",
    /*
     * 这一轮讲的正是工单详情里的东西 → 让详情跟着台词逐段展开。
     * 数量 3 = WorkOrderDetail 的三个可揭示分区（摘要 / 指派 / 环境·下发）。
     */
    reveal: {
      target: "order-detail",
      sections: ["order", "scope", "pending"],
      /* 三段节拍：摘要 → 任务范围 → 后续执行模块（与 `ordersReveal.ts` 的组名对齐） */
      beats: [["order"], ["scope"], ["pending"]],
    },
    nav: { route: "order", order: "current" },
    voicePack: null,
    intentId: "unresolved_followup",
  },
];

/** 按圈号取一轮 */
export function roundByNo(roundNo: string): ScriptRound | null {
  return SCRIPT_ROUNDS.find((r) => r.roundNo === roundNo) ?? null;
}

/** 这一轮要播的主台词（不含备用播报） */
export function mainLineOf(round: ScriptRound): string {
  const main = round.lines.find((l) => l.role === "main");
  return main ? main.text : "";
}

export const SCRIPT_ROUND_COUNT = SCRIPT_ROUNDS.length;



