# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
back
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/assets/sc_specular_map.png
src/assets/sc_ambient_occlusion_map.png

# 构建期素材缓存（tools/build-terrain.mjs 下载的 DEM 瓦片，可再生，不入库）
.cache

# Vite/编辑器在 Windows 上写出的临时目录（chokidar 去 watch 会 EBUSY）
*.tmpdir
.tmp-*

# 无头截图自查产物（tools/shot.mjs）
tmp-shot/


# 共享服务的运行期状态：SQLite 库与磁盘资产都由服务启动时重建，
# 入库会把「本机演示到哪一步了」带进仓库，换台机器反而对不上。
server/data/
server/assets/

# Local SensorTag BLE runtime
.venv-sensortag/
__pycache__/

# 形象展台的构建产物（vite.lab.config.ts 的输出，可由 npm 脚本重建）
dist-lab/
